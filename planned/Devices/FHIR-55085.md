# Implementation Plan: FHIR-55085

**Status:** Halted — required per-repo briefing is missing.

## Ticket

- **Key:** [FHIR-55085](https://jira.hl7.org/browse/FHIR-55085)
- **Title:** Potentially re-useable THO-candidate value sets in resource DeviceMetric
- **Work Group:** Devices
- **Specification:** FHIR Core (FHIR)
- **Resolution:** Persuasive with Modification

## In-Scope Repositories

Based on the ticket resolution (migrate DeviceMetric-bound CodeSystems / ValueSets — `metric-category`, `metric-availability`, `metric-calibration-type`, and the `color-names` / `color-rgb` CodeSystems behind `color-codes` — from FHIR Core to THO), the change touches at least:

1. **HL7/fhir** (FhirCore) — DeviceMetric resource bindings and the source CodeSystem/ValueSet definitions currently authored in core.
2. **HL7/UTG** (Terminology / THO authoring repo) — destination for the migrated CodeSystems and ValueSets.

## Missing Briefing — Stopping per Instructions

Per the orchestrator directive ("If any [briefing] is missing/stale, STOP and write a short note in the output file naming the missing briefing instead of proceeding with partial repo context"), planning is halted.

| Repo | Briefing path | Status |
|------|---------------|--------|
| HL7/fhir | `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` | Present, current (clone HEAD `db79dcd` matches `meta.json`) |
| HL7/UTG | `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` | **Missing** — no `repo-analysis/` directory exists under `cache/github/repos/HL7_UTG/` (only `clone/` is present) |

## Action Required

Run the `repo-analysis` skill for `HL7/UTG` to produce its `briefing.md` + `meta.json`, then re-run `ticket-plan` for `FHIR-55085`. The HL7/fhir briefing does not need refreshing.

## Data Gathered (for re-run)

- Ticket fetched (`get` jira FHIR-55085 with content/comments/snapshot) — saved to `temp/plan/FHIR-55085.json`.
- Cross-references (`cross-referenced` FHIR-55085) — **0 hits**, no GitHub/Zulip/Jira xrefs.
- Keywords (`keywords` jira FHIR-55085) — top hits: `devicemetric`, `calibration`, `availability`, `devicemetric.category`, `devicemetric.color`, `devicemetric.availability`, `devicemetric.calibration.type`, `color`, `metric-category`, `colorcodes`, `colorrgb`, `devicemetricavailability`, `devicemetriccalibrationtype`.
- Resolution comments note: "We do intend to move these. The exception is color, which is owned by FHIR-I. FHIR-I can move color if they want to." — i.e., color-codes / color-names / color-rgb are out of scope for the Devices-driven migration; only `metric-category`, `metric-availability`, `metric-calibration-type` are the WG's responsibility.
