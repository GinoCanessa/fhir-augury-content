# Implementation Plan: FHIR-53036

**Status:** HALTED — required repo briefing missing.

## Ticket Snapshot

| | |
|-|-|
| Ticket | [FHIR-53036](https://jira.hl7.org/browse/FHIR-53036) : Change Request |
| Title | Correct location inconsistency of CSs & VSs used by resource DeviceAlert and extension device-alertDetection. |
| Work Group | Devices |
| Status | Medium · Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive |
| Reporter | Javier Espina |
| Assignee | Elliot Silver |
| Created | 2025-10-01 |
| Updated | 2025-10-18 |
| Resolved | 2025-10-08 |

**Resolution description (verbatim):** "Will update the canonical URLs for the DeviceAlert-related codesystems/valuesets added to the extensions pack to use the same canonicals as the core versions of the terminology. (Actually removing the content and pointing at the core versions will need to wait until R6 publishes.)"

**Linked GitHub work:** [HL7/fhir-extensions#215](https://github.com/HL7/fhir-extensions/pull/215) (referenced in comments — the implementing PR against the extensions pack).

## In-Scope Repositories (inferred)

Although the ticket's `metadata.specification` is `FHIR Core (FHIR)`, the resolution explicitly scopes the change to the **extensions pack** ("the DeviceAlert-related codesystems/valuesets added to the extensions pack"), and the only cross-referenced GitHub artifact is a PR against `HL7/fhir-extensions`. Likely in-scope repositories:

1. **`HL7/fhir-extensions`** — primary. Holds the duplicate DeviceAlert-related CS/VS resources whose canonical URLs must be realigned to the FHIR Core canonicals.
2. **`HL7/fhir`** — secondary / reference only. Source of the canonical URLs that the extensions-pack copies must adopt; per the resolution, no edits are needed in core until R6 publishes (at which point the extensions copies will be removed and pointed at core).

## Halt Reason

Per the ticket-plan skill (Step 3), I must read the persisted per-repo briefing
(`cache/github/repos/<owner>_<name>/repo-analysis/briefing.md` +
`meta.json`) for every in-scope repo before producing an implementation plan,
and I must **not** fabricate repo facts when a briefing is missing.

Briefing status check:

| Repo | `repo-analysis/briefing.md` | `repo-analysis/meta.json` |
|------|-----------------------------|---------------------------|
| `HL7/fhir-extensions` | **MISSING** | **MISSING** |
| `HL7/fhir` | present | present |

The primary in-scope repo (`HL7/fhir-extensions`) has no `repo-analysis/`
artifacts at all (only `clone/` is populated under
`cache/github/repos/HL7_fhir-extensions/`). Without the briefing's
Authoring root(s), Artifact Map, Ticket-Relevant Paths, Recommended Change
Recipes, and Warnings/Gotchas, the implementation plan would have to invent
file paths and conventions for the very repo that owns the change — which
this skill explicitly forbids.

## Requested Action

Please run the `repo-analysis` skill for `HL7/fhir-extensions` (e.g.,
`repo-analysis HL7/fhir-extensions if-stale`) and then re-dispatch
`ticket-plan FHIR-53036`. Once the briefing is in place, the plan can be
grounded in the extensions-pack authoring layout (likely
`input/resources/CodeSystem-*` and `input/resources/ValueSet-*` or
equivalent — to be confirmed from the briefing) and reference
HL7/fhir-extensions#215 for the concrete edit pattern.

No further steps were executed.
