# Implementation Plan: FHIR-45874

**Status:** Halted — required per-repo briefing is missing.

## Ticket Snapshot

| | |
|-|-|
| Ticket | [FHIR-45874](https://jira.hl7.org/browse/FHIR-45874) : Change Request |
| Title | Add .classification |
| Work Group | Pharmacy |
| Status | Medium — Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter / Assignee | Jose Costa-Teixeira / Jose Costa-Teixeira |
| Created / Updated | 2024-05-13 / 2025-10-11 |

**Resolution description (verbatim):**
> We will create a core extension "medication-classification" that echoes the
> MedicationKnowledge.classification backbone.
>
> type 1..1 CodeableConcept
> classification 0..* CodeableConcept
> source[x] 0..1 string|uri

## In-Scope Repositories

Inferred from the Jira cross-references returned by
`fhir-augury-cli cross-referenced FHIR-45874`:

| Repo | Category | Evidence |
|------|----------|----------|
| `HL7/fhir-extensions` | FhirExtensionsPack | Two commits on this repo's clone explicitly reference the ticket and add the new core extension:<br>• `d316c8c8c5cc1f64fd56e18a226f8d2571ebfd36` — *"FHIR-45874"* — adds `StructureDefinition-medication-classification.xml` (129 lines).<br>• `293d6c4380ecb0fcf16d59b1f6a18d8f6db64af8` — *"FHIR-45874 - add bindings and definitions"* — adds `CodeSystem-medication-classification-type.xml`, `ValueSet-medication-classification-type.xml` and updates `StructureDefinition-medication-classification.xml`. |
| `HL7/fhir` | FhirCore | Likely a companion ballot-note / resource-introduction update for the `Medication` resource, since the resolution targets the `Medication` resource. Not yet confirmed against the clone; the authoritative path list must come from the `HL7/fhir` briefing at the time the plan is written. |

## Halt Reason

Per the `ticket-plan` skill (Step 3) and the staleness rules documented in
the `repo-analysis` skill, a per-repo briefing must be present **and fresh**
before any implementation plan can be written. A briefing is fresh only
when *both* the recorded `clone_head_sha` matches the current clone HEAD
**and** the recorded `playbook_sha` matches the current source-controlled
playbook.

Checking the two in-scope repos against
`cache/github/repos/<owner>_<name>/repo-analysis/`:

### `HL7/fhir-extensions` — **briefing missing**

- `cache/github/repos/HL7_fhir-extensions/repo-analysis/briefing.md` — **does not exist**.
- `cache/github/repos/HL7_fhir-extensions/repo-analysis/meta.json` — **does not exist**.
- Clone is populated at `cache/github/repos/HL7_fhir-extensions/clone` (HEAD
  `00df5f8371bcf6d4c897631ddba9f1fef3ed9b0b`), and both of the FHIR-45874
  commits listed above are reachable from that HEAD — so the repo is in
  scope, but no briefing has been produced for it.

This is the primary blocker: the ticket's concrete change (adding the
`medication-classification` core extension, its `CodeSystem`, and its
`ValueSet`) lives entirely in `HL7/fhir-extensions`, and the authoring
root / change-recipe / gotcha data required to ground the plan is not
available in the cache.

### `HL7/fhir` — **briefing present but playbook-stale**

- `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` — exists.
- `cache/github/repos/HL7_fhir/repo-analysis/meta.json`:
  - `clone_head_sha` = `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` — matches
    the current clone HEAD. ✅
  - `playbook_sha` = `9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4` — current
    `.github/skills/repo-analysis/categories/fhir-core.md` is at
    `67043498b4b3ef0c4990b7705c85aa0dbe6fbc7a` (commit *"Pass of skill
    updates"*). ❌ mismatch → **stale** per the `repo-analysis` staleness
    rule.
  - The existing briefing is also scoped to a different ticket
    (`ticket_key: FHIR-42980`) and its *Ticket-Relevant Paths* /
    *Recommended Change Recipes* sections describe FML documentation
    edits on `source/mapping-language.html`, not anything in
    `source/medication/`. It cannot be reused verbatim for this ticket.

## Requested Action

Please run the `repo-analysis` skill to produce/refresh briefings for both
in-scope repos, then re-run the `ticket-plan` skill for `FHIR-45874`.
Suggested invocations:

```text
repo-analysis HL7/fhir-extensions refresh --ticket FHIR-45874
repo-analysis HL7/fhir             refresh --ticket FHIR-45874
```

Or, to refresh only what is actually stale:

```text
repo-analysis HL7/fhir-extensions HL7/fhir if-stale --ticket FHIR-45874
```

Once both `cache/github/repos/HL7_fhir-extensions/repo-analysis/briefing.md`
and `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` are present and
fresh (matching `clone_head_sha` **and** `playbook_sha`), I can ground the
implementation plan in their *Authoring root(s)*, *Ticket-Relevant Paths*,
*Recommended Change Recipes*, and *Warnings / Gotchas* without fabricating
repo facts.

## Data Already Gathered (for the re-run)

All cached under `temp/plan/`:

- `FHIR-45874-get.json` — full ticket with content, comments, snapshot.
- `FHIR-45874-xref.json` — cross-references (3 hits: 2 GitHub commits in
  `HL7/fhir-extensions`, 1 FHIR artifact reference to
  `MedicationKnowledge.classification`).
- `FHIR-45874-kw.json` — keywords (top FHIR-path keywords:
  `medicationknowledge.classification`, `medicationknowledge`,
  `medication`; top words: `classification`, `atc`, `codeableconcept`).

Key data points for the next run:

- **Target resource:** `Medication` (core spec), with the extension's
  shape mirroring `MedicationKnowledge.classification`.
- **New artifacts (already drafted on `HL7/fhir-extensions` master):**
  - `StructureDefinition-medication-classification.xml`
  - `CodeSystem-medication-classification-type.xml`
  - `ValueSet-medication-classification-type.xml`
- **Extension shape per resolution:**
  - `type`       `1..1 CodeableConcept`
  - `classification` `0..* CodeableConcept`
  - `source[x]`  `0..1 string | uri`
- **Open design note from comments** (john.hatem, 2024-09-25): the WG
  discussed using *only* the `classification` element instead of the
  full `MedicationKnowledge.classification` backbone; this was to be
  voted on in the next WG quarter. The plan must reconcile the
  as-resolved shape above against the as-committed shape in the two
  GitHub commits before listing final tasks.
