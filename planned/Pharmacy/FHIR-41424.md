# Implementation Plan: FHIR-41424 — HALTED

| | |
|-|-|
| Ticket | [FHIR-41424](https://jira.hl7.org/browse/FHIR-41424) : Change Request |
| Title | Add flavor and surfaceform to the medicationknowledge-characteristic system |
| Work Group | Pharmacy |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive |
| Status | Medium / Resolved - change required |
| Reporter | Giorgio Cangioli |

## Halt Note

Planning was **halted at Step 3** of the `ticket-plan` skill because a required
per-repo briefing is missing. Per the skill's rules, the plan must not be
fabricated without grounded repo context.

### Scope identified (Steps 1–2)

Based on the ticket resolution ("The binding is an example binding. Will add
flavouring but implementers can add other values such as surface form as
necessary.") and cached clone inspection, the change targets the
`medicationknowledge-characteristic` CodeSystem and its wrapping ValueSet:

- **HL7/UTG** — owns the CodeSystem of record
  (`http://terminology.hl7.org/CodeSystem/medicationknowledge-characteristic`)
  at `input/sourceOfTruth/fhir/codeSystems/CodeSystem-medicationknowledge-characteristic.xml`
  and its ValueSet at
  `input/sourceOfTruth/fhir/valueSets/ValueSet-medicationknowledge-characteristic.xml`.
  This is where the new `flavoring` concept must be added.
- **HL7/fhir** (FhirCore) — carries the example-binding ValueSet at
  `source/medicationknowledge/valueset-medicationknowledge-characteristic.xml`,
  which simply `<include>`s the UTG CodeSystem. It may require no edit, but
  needs confirmation against the briefing's recipes.

Cross-references: `cross-referenced FHIR-41424` returned 0 hits.

### Missing / stale briefings

| Repo | Briefing | Meta | Status |
|------|----------|------|--------|
| HL7/UTG | `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` | `cache/github/repos/HL7_UTG/repo-analysis/meta.json` | **MISSING** — no `repo-analysis/` directory exists under `cache/github/repos/HL7_UTG/` |
| HL7/fhir | `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` | `cache/github/repos/HL7_fhir/repo-analysis/meta.json` | Present and current (clone HEAD `db79dcdf…` matches `meta.json`; playbook SHA `9c2e8e5b…` matches) |

### Action required

Run the `repo-analysis` skill for **HL7/UTG** and then re-invoke
`ticket-plan FHIR-41424`:

```
repo-analysis HL7/UTG if-stale
```

Once the UTG briefing is in place, planning can proceed with:

- Adding a new `<concept>` (code `flavoring`, display "Flavoring",
  definition describing the flavouring/taste additive of the product) to the
  UTG CodeSystem, following UTG's change recipe as documented in that
  briefing.
- Bumping the CodeSystem `<version>` per UTG policy.
- Confirming whether the HL7/fhir example ValueSet needs any regeneration
  or if the include-CodeSystem reference auto-picks up the new code.
- Explicitly recording the WG decision to **not** add `surfaceform`
  (Pharmacy asked for clarification; reporter described it as
  "surface shape — convex/concave/flat" but no consensus was reached, and
  the resolution only commits to `flavouring`).

No tasks, file paths, or recipes will be emitted until the UTG briefing is
available, to avoid fabricating repo facts.
