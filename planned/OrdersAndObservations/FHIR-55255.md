# Implementation Plan: FHIR-55255

**Status:** Halted — required repo briefing missing.

**Resolution:** Persuasive with Modification

This ticket's resolution requires moving multiple CodeSystems and ValueSets
from FHIR Core (`HL7/fhir`) into Terminology.HL7.org content managed in
`HL7/UTG`, plus retargeting/upgrading bindings (and one type change) in
the DeviceDefinition resource. The plan therefore needs both repo
briefings.

The **`HL7/UTG`** per-repo briefing is **missing**:

- Expected: `cache/github/repos/HL7_UTG/repo-analysis/briefing.md`
- Expected: `cache/github/repos/HL7_UTG/repo-analysis/meta.json`
- Found: only `cache/github/repos/HL7_UTG/clone/` exists; no
  `repo-analysis/` directory.

Per the `ticket-plan` skill, this skill is data-only with respect to
`repo-analysis` and must not proceed with partial repo context. Please
run the `repo-analysis` skill for `HL7/UTG` and then re-run this plan:

> Briefing for `HL7/UTG` is missing. Please run `repo-analysis HL7/UTG`
> and let me know when it's ready.

(The `HL7/fhir` briefing is fresh — `meta.json` `playbook_sha`
`9c2e8e5b…` and `clone_head_sha` `db79dcdfe1…` both match the live
values, so no action is needed for that repo.)
