# Implementation Plan: FHIR-42844

**Halted at Step 3 (briefing freshness).**

The repo briefing for **`HL7/UTG`** is missing
(`cache/github/repos/HL7_UTG/repo-analysis/briefing.md` does not exist).
This ticket's resolution ("Remove the sentence \"Not under direct
supervision of practitioner such as a physical therapist. (e.g., laps
swum, steps, sleep data)\" as requested") changes the definition of the
`activity` concept in the `observation-category` CodeSystem, which lives
in `HL7/UTG` at
`input/sourceOfTruth/fhir/codeSystems/CodeSystem-observation-category.xml`
(label on the ticket: `UTG_tracker_needed`; comments confirm OO will
shepherd the change through UTG).

Please run the `repo-analysis` skill for `HL7/UTG` and re-dispatch this
ticket once the briefing is in place. The `HL7/fhir` briefing is fresh
and does not need refreshing.
