# Implementation Plan: FHIR-55279

**Status:** Halted — required per-repo briefing missing.

## Reason

This ticket's resolution moves the `task-code` CodeSystem out of FHIR Core and
into THO (the `HL7/UTG` repository), while the `task-code` ValueSet stays in
core. The other five CodeSystem/ValueSet pairs are flagged `experimental` in
core. Producing a complete plan therefore requires the saved per-repo briefing
for **`HL7/UTG`** in addition to `HL7/fhir`.

The HL7/fhir briefing is fresh, but the HL7/UTG briefing is missing:

- `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` — **not present**
- `cache/github/repos/HL7_UTG/repo-analysis/meta.json` — **not present**

Only `cache/github/repos/HL7_UTG/clone/` exists.

Per the `ticket-plan` skill (Step 3), this skill is data-only with respect to
`repo-analysis` and must not proceed with partial repo context or invent repo
facts. Please run the `repo-analysis` skill for `HL7/UTG` and then re-run
ticket planning for FHIR-55279:

> Briefing for `HL7/UTG` is missing. Please run `repo-analysis HL7/UTG` and
> let me know when it's ready.

## Ticket Snapshot (for context)

| | |
|-|-|
| Ticket | [FHIR-55279](https://jira.hl7.org/browse/FHIR-55279) : Change Request |
| Title | Potentially re-useable THO-candidate value sets in resource Task |
| Work Group | Orders & Observations |
| Status | Highest — Resolved - change required |
| Resolution | Persuasive with Modification |
| Labels | R6-Release-note, Ready_to_Apply, THO-candidate |
| Specification | FHIR Core (FHIR) |
| Reporter | Lloyd McKenzie |
| Assignee | Unassigned |

### Resolution (verbatim)

We will make the following changes:

- `task-status-reason` — change to `experimental` for CodeSystem and ValueSet
- `task-business-status` — change to `experimental` for CodeSystem and ValueSet
- `task-code` — move just the CodeSystem to THO, keep ValueSet `extensible`
- `task-performer-function-code` — change to `experimental` for CodeSystem and ValueSet
- `task-reason` — change to `experimental` for CodeSystem and ValueSet
- `task-inputoutput-parameter-type` — change to `experimental` for CodeSystem and ValueSet

### In-Scope Repositories (inferred)

- `HL7/fhir` (FhirCore) — briefing present and fresh.
- `HL7/UTG` (Utg) — briefing **missing**; required because the `task-code`
  CodeSystem migrates here.
