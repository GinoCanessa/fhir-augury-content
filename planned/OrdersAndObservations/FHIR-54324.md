# Implementation Plan: FHIR-54324

**Status:** STOPPED — required repo briefing is stale.

## Briefing gap

The cached per-repo briefing for the in-scope repository is stale and must be
regenerated before this plan can be produced.

| Repo | Briefing path | Issue |
|------|---------------|-------|
| HL7/fhir | `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` | `meta.json.playbook_sha` = `9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4` does not match the current `.github/skills/repo-analysis/categories/fhir-core.md` SHA (`6704349`). Briefing is also ticket-scoped to FHIR-42980 (FML doc fixes) and does not cover Device.availabilityStatus. |
| HL7/UTG | `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` | **Missing.** Resolution coordinates with FHIR-55253 (THO move of the availabilityStatus value set / code system), so HL7/UTG is plausibly in scope. |

## Action requested

Please run the `repo-analysis` skill for both repos before re-running
`ticket-plan FHIR-54324`:

- `repo-analysis HL7/fhir if-stale` (or refresh, scoped to FHIR-54324 / Device)
- `repo-analysis HL7/UTG if-stale`

Once the briefings are current, this skill can produce the full
implementation plan.

## Ticket context (for reference)

- **Ticket:** [FHIR-54324](https://jira.hl7.org/browse/FHIR-54324) — *status includes `unknown` but availabilityStatus does not*
- **Work Group:** Orders & Observations
- **Specification:** FHIR Core (FHIR)
- **Resolution:** Persuasive with Modification — change `Device.availabilityStatus` to a **preferred** binding and replace the value-set contents with the agreed ~18-code list (`available`, `in-use`, `reserved`, `cleaning`, `sterilizing`, `maintenance`, `calibration`, `charging`, `faulty`, `quarantined`, `recalled`, `out-of-service`, `retired`, `disposed`, `lost`, `in-transit`, `received-not-ready`, `unknown`). Coordinate with FHIR-55253 (move of the value set / code system to THO).
- **Labels:** R6-Release-note, Ready_to_Apply, THO-candidate
