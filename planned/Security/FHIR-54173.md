# Implementation Plan: FHIR-54173 — HALT

**Ticket:** [FHIR-54173](https://jira.hl7.org/browse/FHIR-54173) — Change Security Classification Label from "Not Classified" to "Varies"
**Work Group:** Security
**Specification:** FHIR Core (FHIR)
**Resolution:** Persuasive with Modification
**Resolved:** 2026-02-19

## Halt: Required per-repo briefing missing

Step 3 of the `ticket-plan` skill requires a current
`repo-analysis/briefing.md` + `meta.json` for every in-scope repository.
For this ticket, one of the in-scope repos has no briefing, so planning
has been halted rather than fabricating repo facts.

### In-scope repositories (inferred in Step 2)

| Repo | Rationale | Briefing status |
|------|-----------|-----------------|
| `HL7/fhir-extensions` | The resolution and the last work-group comment explicitly target the `resource-security-category` CodeSystem, ValueSet and StructureDefinition, which live in the FHIR Extensions Pack (`http://hl7.org/fhir/extensions/CodeSystem-resource-security-category.html`, `ValueSet-resource-security-category.html`, `StructureDefinition-structuredefinition-security-category.html`). Work-group comment: *"Update the CodeSystem associated with the extension … [links to extensions pack]."* | **MISSING** — no `cache/github/repos/HL7_fhir-extensions/repo-analysis/briefing.md` |
| `HL7/fhir` | The work-group comment says *"update all 53 resources in FHIR core that use the current code."* The authoritative Resolution Description is narrower (*"next publications will use the vocabulary that is in the extension pack"*), but FHIR core may still need to regenerate/pick up the new display on per-resource security-category headers. | Present (`cache/github/repos/HL7_fhir/repo-analysis/briefing.md`). |

### Ticket context captured (for resumption)

- **Type:** Change Request · **Priority:** Medium · **Status:** Resolved - change required
- **Reporter:** Eric Haas · **Assignee:** John Moehrke
- **Resolution Description (authoritative):**
  > The current publications are using the definition of the codes at FHIR R5; Grahame assures me that next publications will use the vocabulary that is in the extension pack. The code has a better display value.
  >
  > | not-classified | No Dominant Category |
- **Work-group follow-up comments (2026-01-19):**
  - *"Agreed that 'Not Classified' is giving the wrong impression. Change the code to 'varies'."*
  - *"wg prefers 'Varied' vs 'Varies' as Varies seems to indicate changes overtime."*
  - *"Update the CodeSystem associated with the extension … And update all 53 resources in FHIR core that use the current code. Unclear if I can use an alias code."*
- **Publisher angle (Moehrke 2026-01-19):** the observed "Not Classified" string is produced by the **IG publisher** when rendering the security-category header, not by the code's display. Changing publisher behavior may be an alternative/adjunct to a vocabulary change.
- **Cross-references:** none found (`cross-referenced FHIR-54173` → no results).
- **Linked issues:** none (`get_jira_item_links` → `[]`).
- **Keywords (top):** classified, classificiation, dominant, vary, security, label, category, psychotherapy, sensitive — consistent with a vocabulary-display change on `resource-security-category`.

### Action required before planning can resume

Please run the `repo-analysis` skill for the missing repo, then re-run
`ticket-plan FHIR-54173`:

```bash
repo-analysis HL7/fhir-extensions if-stale
```

If it turns out `HL7/fhir-extensions` is not actually in scope (e.g.,
the group elects the publisher-only fix, or decides the resolution is
purely informational because the extension pack already carries a
better display), re-run `ticket-plan` with a note to that effect so
the plan can be produced against `HL7/fhir` alone.

*No fabricated file paths, recipes, or tasks are included in this
report, per the `ticket-plan` skill's grounding rules.*
