# Implementation Plan: FHIR-51975 — HALTED

**Title:** Clarify language about required bindings in relation to CodeableConcept
**Work Group:** Terminology Infrastructure
**Specification:** FHIR Core (FHIR)
**Resolution:** Persuasive (change required)

## Halt reason

Per the `ticket-plan` skill, Step 3 requires a current per-repo briefing for every in-scope repository. The only repository in scope for this ticket is `HL7/fhir` (FHIR Core spec — the resolution is a narrative clarification to the binding-strength text in the Terminologies documentation). Its saved briefing is **stale**:

- Briefing path: `cache/github/repos/HL7_fhir/repo-analysis/briefing.md`
- Metadata path: `cache/github/repos/HL7_fhir/repo-analysis/meta.json`
- `meta.json.clone_head_sha` = `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` — **matches** current clone HEAD ✔
- `meta.json.playbook_sha` = `9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4` — **does not match** current source-controlled `.github/skills/repo-analysis/categories/fhir-core.md` at `67043498b4b3ef0c4990b7705c85aa0dbe6fbc7a` ✘

Per `repo-analysis/SKILL.md` staleness rules, any mismatch (clone HEAD **or** playbook SHA) marks the briefing stale. The `ticket-plan` skill explicitly forbids proceeding with partial repo context or fabricating repo facts.

## Requested action

Please run:

```
repo-analysis HL7/fhir if-stale
```

(or `repo-analysis HL7/fhir refresh` to force a rebuild), then re-dispatch planning for **FHIR-51975**.

## Data already gathered (for the re-run)

- **Resolution:** Persuasive. Replace the binding text
  > "When an element is bound to a required value set, derived profiles may state rules on which codes can be used, including removing codes from allowed use, but cannot specify additional codes for these elements."

  with

  > "When an element is bound to a value set with required binding strength, derived profiles can remove concept codes and cannot specify additional concept codes to be used. This does not prevent additional codes for other instances of CodeableConcept.coding in the same element."
- **Cross-references:** 1 Zulip thread — `#committers > Text about CodeableConcept required binding` (stream 179165). No GitHub PRs/issues/commits cross-reference this ticket.
- **Keywords (top):** codeableconcept, codeableconcept.coding, binding, coding, bound, required, derive, additional, concept, strength.
- **Scope inference:** Documentation-only change to the Terminologies narrative in FHIR Core (`HL7/fhir`). No second repo identified.

Ticket data is cached in `temp/plan/FHIR-51975-ticket.json`, `FHIR-51975-xrefs.json`, and `FHIR-51975-keywords.json` under the working directory so a follow-up run can skip the Jira fetches.
