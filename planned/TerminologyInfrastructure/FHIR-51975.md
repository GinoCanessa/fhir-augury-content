# Implementation Plan: FHIR-51975

## Ticket Summary

| Field | Value |
|-------|-------|
| **Ticket** | [FHIR-51975](https://jira.hl7.org/browse/FHIR-51975) |
| **Title** | Clarify language about required bindings in relation to CodeableConcept |
| **Type** | Change Request |
| **Priority** | Medium |
| **Status** | Resolved – change required |
| **Resolution** | Persuasive |
| **Work Group** | Terminology Infrastructure |
| **Specification** | FHIR Core (FHIR) |
| **Labels** | Binding, TEXTONLY |
| **Reporter** | Rik Smithies |

## Problem Statement

The current text in the "Required" binding strength section of the FHIR terminologies page states:

> "When an element is bound to a required value set, derived profiles may state rules on which codes can be used, including removing codes from allowed use, but cannot specify additional codes for these elements."

This language is ambiguous when applied to `CodeableConcept`. Because a `CodeableConcept` can have multiple `coding` entries, readers may incorrectly interpret this as prohibiting *any* additional codes within the `CodeableConcept`—including codes in other `coding` instances that are not governed by the required binding. In practice, the required binding only constrains the binding-matched coding; additional codings representing the same concept from other code systems are permitted.

The Zulip discussion (committers > "Text about CodeableConcept required binding") between Rik Smithies and Grahame Grieve confirmed this is a wording issue, and Grahame agreed the text "could be worded better."

## Resolution Description (from Jira)

Update the statement from:

> "When an element is bound to a required value set, derived profiles may state rules on which codes can be used, including removing codes from allowed use, but cannot specify additional codes for these elements."

To:

> "When an element is bound to a value set with required binding strength, derived profiles can remove concept codes and cannot specify additional concept codes to be used. This does not prevent additional codes for other instances of CodeableConcept.coding in the same element."

## Cross-References

| Source | Reference | Relationship |
|--------|-----------|--------------|
| Zulip | committers > "Text about CodeableConcept required binding" | Origin discussion; 7 messages between Rik Smithies and Grahame Grieve confirming the ambiguity |

## Feature Proposal

This is a **text-only** clarification (no schema, validation, or behavioral changes). The existing paragraph about required-binding constraints on derived profiles will be reworded to:

1. Use more precise terminology ("value set with required binding strength" instead of "required value set").
2. Use "concept codes" instead of "codes" to clarify scope.
3. Add an explicit sentence stating that additional codes in other `CodeableConcept.coding` instances remain permitted.

## Impact Analysis

### Scope: Minimal — Text-Only Change

- **No schema or validation changes** — the underlying rules are unchanged.
- **No code-system or value-set changes** — UTG is not affected.
- **No profile or extension changes** — `fhir-extensions` is not affected.
- **Backward compatible** — this is a clarification of existing intent, not a rule change.

### Risk Assessment: Low

- The change corrects a known source of misinterpretation without altering conformance requirements.
- The resolution was voted "Persuasive" with clear replacement text provided.

## Affected Repositories

| Repository | Affected | Reason |
|------------|----------|--------|
| **HL7/fhir** | ✅ Yes | Contains the source text to update |
| HL7/UTG | ❌ No | No terminology artifacts affected |
| HL7/fhir-extensions | ❌ No | No extensions affected |

## Detailed Implementation Plan

### Repository: HL7/fhir

#### Change 1: Update required-binding paragraph in `source/terminologies.html`

**File:** `source/terminologies.html`
**Lines:** 559–562 (approximate — verify at implementation time)

**Current text (lines 559–562):**
```html
<p>
When an element is bound to a required value set, <a href="profiling.html">derived profiles</a>
may state rules on which codes can be used, including removing codes from allowed use, but cannot specify additional codes for these elements.
</p>
```

**Proposed replacement:**
```html
<p>
When an element is bound to a value set with required binding strength, <a href="profiling.html">derived profiles</a>
can remove concept codes and cannot specify additional concept codes to be used. This does not prevent additional codes
for other instances of <code>CodeableConcept.coding</code> in the same element.
</p>
```

**Rationale:**
- Matches the approved resolution text exactly.
- Replaces "required value set" with "value set with required binding strength" for precision.
- Adds explicit carve-out for additional `CodeableConcept.coding` instances, resolving the ambiguity.
- Wraps `CodeableConcept.coding` in `<code>` tags consistent with the surrounding HTML style.

### Verification

1. **Visual review:** Build the specification pages and confirm the updated text renders correctly on the `terminologies.html#required` section.
2. **Link check:** Verify the `<a href="profiling.html">derived profiles</a>` link still resolves correctly (unchanged).
3. **Contextual consistency:** Review the surrounding CodeableConcept rules section (lines 528–534) to confirm the new sentence is consistent with the existing bullet points about CodeableConcept required bindings (particularly "one of the Coding values SHALL be from the specified value set").

## Files to Modify

| # | File | Change Type | Description |
|---|------|-------------|-------------|
| 1 | `source/terminologies.html` | Edit (text) | Replace required-binding derived-profile paragraph (~lines 559–562) with clarified wording per resolution |

## Testing Considerations

- This is a TEXTONLY change; no automated test changes are required.
- Manual review of the rendered HTML page is sufficient for verification.
- Search the rest of the specification for any cross-references to this specific paragraph text to ensure consistency (none found in current search).
