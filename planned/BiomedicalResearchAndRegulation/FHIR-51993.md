# FHIR-51993: Typo - reprsented

## Ticket Summary

| Field | Value |
|-------|-------|
| **Ticket** | [FHIR-51993](https://jira.hl7.org/browse/FHIR-51993) |
| **Title** | Typo - reprsented |
| **Status** | Resolved - change required |
| **Type** | Technical Correction |
| **Priority** | Medium |
| **Resolution** | Persuasive |
| **Work Group** | Biomedical Research & Regulation |
| **Specification** | FHIR to OMOP FHIR IG (FHIR) |
| **Reporter** | Brett Marquard |
| **Created** | 2025-08-26 |
| **Resolved** | 2025-09-16 |

## Description

The reporter requests that the IG be run through a spell checker. The title specifically calls out the typo "reprsented" (missing the first 'e' in "represented").

## Resolution Description

> We will fix any typos and grammar errors that we missed.

## Implementation Required

**Yes** — Resolution is **Persuasive**, which requires implementation.

---

## Feature Proposal

Fix the typo "reprsented" → "represented" in two markdown files within the FHIR to OMOP IG. Additionally, a missing period/full-stop should be inserted in the same sentence (after "in the table below"), as both files contain an identical run-on sentence.

---

## Impact Analysis

### Scope

- **Change type:** Documentation-only (narrative text correction)
- **Breaking changes:** None
- **Backward compatibility:** Fully backward compatible
- **Risk:** Minimal — purely cosmetic text fix with no semantic or structural impact

### Affected Repository

| Repository | URL |
|------------|-----|
| `HL7/fhir-omop-ig` | https://github.com/HL7/fhir-omop-ig |

> **Note:** This repository (`HL7/fhir-omop-ig`) is **not** in the local cache (`cache/github/repos/`). Changes must be made directly via PR to the GitHub repository.

### Affected Files

| # | File Path | Change |
|---|-----------|--------|
| 1 | `input/pagecontent/StructureMap-ObservationMap-intro.md` | Fix typo "reprsented" → "represented"; add missing period after "table below" |
| 2 | `input/pagecontent/StructureMap-MeasurementMap-intro.md` | Fix typo "reprsented" → "represented"; add missing period after "table below" |

### Files Confirmed Not Affected

No other files in the repository contain the string "reprsented". The typo appears only in the two files listed above (confirmed via GitHub code search).

---

## Detailed Implementation Plan

### Step 1: Fix `StructureMap-ObservationMap-intro.md`

**File:** `input/pagecontent/StructureMap-ObservationMap-intro.md`

**Current text (line in "A Domain-Based Mapping Strategy" section):**
```
At a high-level, mapping rules can be aligned with OMOP domains as reprsented in the table below However, the nuanced distinction...
```

**Corrected text:**
```
At a high-level, mapping rules can be aligned with OMOP domains as represented in the table below. However, the nuanced distinction...
```

**Changes:**
1. `reprsented` → `represented` (fix typo — missing first 'e')
2. `below However` → `below. However` (add missing period to fix run-on sentence)

### Step 2: Fix `StructureMap-MeasurementMap-intro.md`

**File:** `input/pagecontent/StructureMap-MeasurementMap-intro.md`

**Current text (identical paragraph in "A Domain-Based Mapping Strategy" section):**
```
At a high-level, mapping rules can be aligned with OMOP domains as reprsented in the table below However, the nuanced distinction...
```

**Corrected text:**
```
At a high-level, mapping rules can be aligned with OMOP domains as represented in the table below. However, the nuanced distinction...
```

**Changes:**
1. `reprsented` → `represented` (fix typo — missing first 'e')
2. `below However` → `below. However` (add missing period to fix run-on sentence)

### Step 3: Broader Spell-Check (Recommended)

The reporter's original request was to "run guide through a quick spell checker." While the ticket title specifically identifies "reprsented," the resolution commits to fixing "any typos and grammar errors that we missed." The implementer should consider running a spell-check tool (e.g., `cspell`, `aspell`, or a VS Code spell checker) across all `input/pagecontent/*.md` files to identify additional issues beyond the two instances flagged here.

---

## Testing & Verification

1. **Text verification:** Confirm the string "reprsented" no longer appears in any file in the repository
2. **IG build:** Run `_genonce.sh` or `_genonce.bat` to verify the IG builds successfully after the change
3. **Visual review:** Confirm the corrected text renders properly on the affected pages (ObservationMap intro and MeasurementMap intro)

---

## Cross-References

No cross-references were found for this ticket (no related Jira tickets, Zulip threads, or GitHub issues/PRs).

---

## Notes

- Both affected files share an identical top section (Observation Resource Considerations / Domain-Based Mapping Strategy) containing the typo. `StructureMap-MeasurementMap-intro.md` has additional content below (Unit Handling and Standardization section) that `StructureMap-ObservationMap-intro.md` does not.
- The duplicate content across these two files may itself warrant review in a separate ticket, but that is out of scope for this correction.
