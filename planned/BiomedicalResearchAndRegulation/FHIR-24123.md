# Implementation Plan: FHIR-24123

**Title:** Corect spelling of 'transforming' in paragraph: - CDISC_LAB #2
**Status:** Resolved - change required
**Resolution:** Persuasive
**Work Group:** Biomedical Research & Regulation
**Specification:** CDISC Lab (FHIR)
**Resolved:** 2021-10-27

---

## Resolution Summary

This is a **Technical Correction** ticket requesting a simple typo fix. The word "tranforming" (missing the first 's') should be corrected to "transforming" in the "Purpose and Goals of the Project" section of the CDISC Lab Implementation Guide overview page.

The resolution is **Persuasive** — the ballot comment was accepted as-is. The vote was 17-0-0 (Christi Denny / Myron Finseth), classified as non-substantive.

**Current status: This fix appears to have already been applied.** The current `master` branch of `HL7/cdisc-lab` contains the correct spelling "transforming" in `source/pages/overview.md`. The fix was most likely applied in commit `d4eb4997` ("Updates per Sept 2019 ballot comments", 2020-05-08) by CDenney.

## Feature Proposal

### Problem Statement

The overview page of the CDISC Lab Implementation Guide contained a typo in the "Purpose and Goals of the Project" section. The word "tranforming" was misspelled — it was missing the letter 's', and should read "transforming".

**Existing (incorrect) wording:**
> The purpose of the FHIR to CDISC Laboratory Data Mapping project is to provide organizations with information on consuming data that have been supplied in a FHIR format, and **tranforming** the data into the CDISC standards that are required for research sponsors.

**Proposed (corrected) wording:**
> The purpose of the FHIR to CDISC Laboratory Data Mapping project is to provide organizations with information on consuming data that have been supplied in a FHIR format, and **transforming** the data into the CDISC standards that are required for research sponsors.

### Proposed Change

A single-word correction: change "tranforming" → "transforming" in `source/pages/overview.md`.

### Design Rationale

Straightforward typo correction. No semantic or structural changes are involved. The fix improves readability and professionalism of the published specification.

## Impact Analysis

### Affected Repositories

#### HL7/cdisc-lab (Implementation Guide — not in local cache)

- **Role:** Primary repository for the CDISC Lab Implementation Guide. Contains the narrative content, profiles, and build configuration for the IG.
- **Affected Files:**
  - `source/pages/overview.md` — Line containing the "Purpose and Goals of the Project" paragraph (under the `### Purpose and Goals of the Project` heading)
- **Change Scope:** Minor — single character insertion (adding 's' to "tranforming" → "transforming")
- **Note:** This repository is **not** in the local cache (`cache/github/repos/`). The file was verified via the GitHub API. The current `master` branch already contains the corrected spelling, suggesting this fix was applied in the May 2020 batch update commit (`d4eb4997`).

### Breaking Changes

None. This is a documentation-only typo fix with zero impact on profiles, terminology, or machine-readable content.

### Related Specifications

- **CDISC Lab (FHIR)** — The directly affected implementation guide.
- No other specifications are affected.

### Related Tickets

| Ticket | Title | Relevance |
|--------|-------|-----------|
| FHIR-43876 | Sentence sounds "wrong" | Related by keywords (different IG: LIVD, different work group: O&O) |
| FHIR-15426 | "Scope" page spelling and grammar corrections | Similar typo-fix pattern in another IG |
| FHIR-24135 | Please change 'lab' to 'laboratory' | Same ballot cycle, related CDISC Lab wording |

No cross-references (incoming or outgoing) were found for FHIR-24123.

### Terminology Impact

None. This change does not affect any ValueSets, CodeSystems, ConceptMaps, or terminology bindings.

## Implementation Plan

### Prerequisites

1. Clone or access the `HL7/cdisc-lab` repository (not in local cache).
2. Verify whether the fix has already been applied on the target branch.

### Step-by-Step Tasks

> **⚠️ Likely Already Complete:** The current `master` branch of `HL7/cdisc-lab` already contains "transforming" (correct spelling) in `source/pages/overview.md`. The implementation steps below are provided for completeness, but the fix appears to have been applied in commit `d4eb4997` (2020-05-08).

1. **Verify current state** — Check `source/pages/overview.md` in the target branch for the string "tranforming". If not found, the fix is already applied.

2. **If fix is still needed:**
   - Open `source/pages/overview.md`
   - Locate the paragraph under `### Purpose and Goals of the Project`
   - Change `tranforming` → `transforming`
   - Commit with message: `fix: correct spelling of 'transforming' in overview (FHIR-24123)`

3. **Rebuild the IG** — Run `_genonce.sh` (or equivalent) to regenerate the published HTML and verify the fix appears in the rendered output.

### Validation Checklist

- [x] StructureDefinition(s) validate with no errors — N/A (no StructureDefinition changes)
- [x] Element definitions include short description and formal definition — N/A
- [x] Cardinality is correct and consistent with the resolution — N/A
- [x] Type constraints match the intended design — N/A
- [x] Terminology bindings reference valid ValueSets — N/A
- [x] Search parameters updated if the change adds searchable elements — N/A
- [x] Examples updated to demonstrate the new/changed elements — N/A
- [x] Resource scope/boundaries documentation updated if resource scope changed — N/A
- [x] Cross-references to other resources are bidirectional — N/A
- [x] No regressions in existing invariants or constraints — N/A

### Testing Considerations

- **Visual inspection:** Verify the corrected word "transforming" appears in the rendered HTML output of the IG overview page.
- **Full-text search:** Run `grep -r "tranforming" source/` to confirm no other instances of the typo remain in the repository.
- **IG build:** Run the IG publisher to ensure no build errors are introduced (though this change should have zero risk).

### Open Questions

1. **Is this fix already applied?** The current `master` branch appears to contain the corrected spelling. If so, this ticket may only require verification and closure — no additional code change is needed.
2. **Jira status discrepancy:** The ticket status is "Resolved - change required" (not "Applied"), yet the fix appears to be in the repository. The Jira ticket status may need to be updated to "Applied" if the fix is confirmed as already present.
