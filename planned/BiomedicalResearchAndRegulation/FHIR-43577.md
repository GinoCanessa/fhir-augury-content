# Implementation Plan: FHIR-43577

**Halt — required repo briefing missing.**

The resolution for [FHIR-43577](https://jira.hl7.org/browse/FHIR-43577) ("Change research-subject-state value set in accordance with BRR consensus", resolution **Persuasive with Modification**) explicitly states:

> We need to update the underlying code system for this value set which will require a UTG ticket to be created. The new codes will come from the table included in this issue. NOTE: the table included has codes but not display names. Display names will need to be created in the UTG ticket.

The bound ValueSet/CodeSystem is `http://terminology.hl7.org/CodeSystem/research-subject-state` (referenced from `ResearchSubject.subjectState.code` in `cache/github/repos/HL7_fhir/clone/source/researchsubject/structuredefinition-ResearchSubject.xml`). Its source-of-truth lives in **HL7/UTG** at `input/sourceOfTruth/fhir/codeSystems/CodeSystem-research-subject-state.xml` (and the matching ValueSet under `input/sourceOfTruth/fhir/valueSets/`), so HL7/UTG is an in-scope repository for this ticket.

No per-repo briefing exists for **HL7/UTG**:

- Missing: `cache/github/repos/HL7_UTG/repo-analysis/briefing.md`
- Missing: `cache/github/repos/HL7_UTG/repo-analysis/meta.json`

Per the `ticket-plan` skill (Step 3), planning cannot proceed with partial repo context and must not fabricate repo facts. Please run the `repo-analysis` skill for `HL7/UTG` (e.g., `repo-analysis HL7/UTG if-stale`) and re-dispatch this ticket. The HL7/fhir briefing is fresh and ready; only UTG is blocking.
