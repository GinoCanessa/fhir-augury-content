# Implementation Plan: FHIR-31034 — HALTED

**Ticket:** [FHIR-31034](https://jira.hl7.org/browse/FHIR-31034) — *add values to careTeam role*
**Work Group:** Financial Mgmt
**Specification:** FHIR Core (FHIR)
**Resolution:** Persuasive with Modification

## Halt reason

The ticket asks to add new concepts to the `Claim Care Team Role` terminology.
The ValueSet `claim-careteamrole` in `HL7/fhir` composes from the CodeSystem
`http://terminology.hl7.org/CodeSystem/claimcareteamrole`, whose source of
truth lives in `HL7/UTG`:

- `HL7/fhir` — `source/claim/valueset-claim-careteamrole.xml` (include system
  `http://terminology.hl7.org/CodeSystem/claimcareteamrole`, lines 35–39).
- `HL7/UTG` — `input/sourceOfTruth/fhir/codeSystems/CodeSystem-claimcareteamrole.xml`
  (authoritative CodeSystem) and
  `input/sourceOfTruth/fhir/valueSets/ValueSet-claim-careteamrole.xml`.

`HL7/UTG` is therefore an in-scope repository for this change. Per the
`ticket-plan` skill Step 3 (briefing check), the plan cannot be drafted
without a current per-repo briefing for every in-scope repo, and fabricating
repo facts is explicitly forbidden.

**Missing briefing:**

- `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` — not present
- `cache/github/repos/HL7_UTG/repo-analysis/meta.json` — not present

The `HL7/fhir` briefing at
`cache/github/repos/HL7_fhir/repo-analysis/briefing.md` is present and
current (clone HEAD `db79dcd` and playbook SHA `9c2e8e5` both match
`meta.json`), but it is scoped to ticket `FHIR-42980` and its
*Ticket-Relevant Paths* do not cover the `source/claim/` terminology
artifacts touched by this ticket. The general Facts / Recipes / Warnings
remain usable, but the UTG gap is the blocking issue.

## Requested action

Please run the `repo-analysis` skill for `HL7/UTG` (e.g.,
`repo-analysis HL7/UTG if-stale` or equivalent) so that
`cache/github/repos/HL7_UTG/repo-analysis/briefing.md` and `meta.json` exist
and match the current clone HEAD. Once the briefing is in place, re-run
`ticket-plan FHIR-31034` and I will produce the full implementation plan,
grounded in the UTG briefing plus the existing FhirCore briefing.

## What the plan will cover (preview, once unblocked)

Resolution text from Jira: *"'Performing' is already captured as 'rendering'."*
Taken together with the ticket description, the approved additions to
`CodeSystem/claimcareteamrole` will be:

| Code | Display | Definition |
|------|---------|-----------|
| `attending` | Attending | The attending physician |
| `referring` | Referring | The referring physician |
| `operating` | Operating | The operating physician |
| `otheroperating` | Other Operating | The other operating physician |
| `prescribing` | Prescribing provider | The prescribing provider |
| `purchasedservice` | Purchased Service | A purchased service occurs when one provider purchases a service from another provider and then provides it to the patient, e.g. a diagnostic exam |

`performing` is explicitly excluded by the resolution (already covered by the
existing `rendering` code).

Likely touch points (to be confirmed against the UTG briefing):

- `HL7/UTG` — add the six concepts to
  `input/sourceOfTruth/fhir/codeSystems/CodeSystem-claimcareteamrole.xml`
  (content = `complete`, so `count` and any expansion/publisher artefacts
  will also need adjustment per UTG conventions).
- `HL7/UTG` — verify
  `input/sourceOfTruth/fhir/valueSets/ValueSet-claim-careteamrole.xml`
  picks the new codes up automatically (include by system, no explicit
  concept list expected).
- `HL7/fhir` — no edit expected in `source/claim/valueset-claim-careteamrole.xml`
  beyond a possible version bump driven by the normal publish cycle; this
  ValueSet includes the system without enumerating concepts.

## Data gathered so far

- `fhir-augury-cli get jira FHIR-31034` — retrieved (no comments on ticket).
- `fhir-augury-cli cross-referenced FHIR-31034` — 0 hits.
- `fhir-augury-cli keywords jira FHIR-31034` — dominant terms: `careteam`
  (fhir_path), `operate`, `purchase`, `attending`, `physician`, `prescribe`,
  `otheroperating`, `purchasedservice`, `rendering`, `refer` — consistent
  with the CodeSystem additions above.
- `HL7/fhir` briefing verified current (clone HEAD / playbook SHA match).
- `HL7/UTG` briefing **missing** — blocking.
