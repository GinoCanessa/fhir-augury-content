# Implementation Plan: FHIR-50907

**Status:** Halted — required repo briefing missing.

The resolution for FHIR-50907 ("Change funds reserve to standard CodeSystem in
THO") explicitly requires:

1. Converting the `funds-reserve` CodeSystem to a *Standard* CodeSystem.
2. **Transferring it to THO** (terminology.hl7.org), which is published from the
   `HL7/UTG` repository.
3. Making the binding *extensible*.

This makes both `HL7/fhir` (current home of the CodeSystem and binding) and
`HL7/UTG` (target home after transfer) in-scope repositories for the plan.

The per-repo briefing for `HL7/UTG` is **missing** — there is no
`cache/github/repos/HL7_UTG/repo-analysis/briefing.md` (only the `clone/`
directory exists).

Per the `ticket-plan` skill (Step 3), this skill is data-only with respect to
`repo-analysis` and must not proceed with partial repo context. Please run:

```
repo-analysis HL7/UTG
```

…and re-run the plan once the briefing is available.

The `HL7/fhir` briefing was verified fresh (`playbook_sha`
`9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4`, `clone_head_sha`
`db79dcdfe196d35bd0e74c58c655a4c1c8f534f7`).
