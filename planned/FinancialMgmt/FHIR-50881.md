# Implementation Plan: FHIR-50881

**Status:** Halted — required per-repo briefing missing.

The resolution for [FHIR-50881](https://jira.hl7.org/browse/FHIR-50881)
("Create CodeSystem and ValueSet to replace Ex-DiagnosisType") explicitly
requires changes in **two** repositories:

1. **HL7/fhir** — modify `Claim.diagnosis.type` and
   `ExplanationOfBenefit.diagnosis.type` bindings to point at the new
   ValueSet, with binding strength `Extensible`.
2. **HL7/UTG** — create a new THO-hosted `DiagnosisType` CodeSystem
   (inheriting all `Ex-DiagnosisType` codes except `Self`, plus new
   codes `PatientReported`, `Other`, `Secondary`) and the corresponding
   ValueSet.

Per Step 3 of the `ticket-plan` skill, a current per-repo briefing must
exist for every in-scope repository before planning can proceed.

- `HL7/fhir` briefing: **present** at
  `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` (fresh —
  `playbook_sha` and `clone_head_sha` match live values).
- `HL7/UTG` briefing: **missing**. The directory
  `cache/github/repos/HL7_UTG/repo-analysis/` does not exist; only the
  `clone/` subdirectory is present.

Because the THO CodeSystem creation (the primary work item in this
resolution) lives in `HL7/UTG`, planning cannot proceed without that
briefing.

## Action required

Please run the `repo-analysis` skill for `HL7/UTG` and re-dispatch this
ticket:

> repo-analysis HL7/UTG

Once `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` and
`meta.json` are in place (with `clone_head_sha` matching the current
clone HEAD and `playbook_sha` matching the live playbook), this plan
can be regenerated.

## Ticket Summary (for context)

| | |
|-|-|
| Ticket | [FHIR-50881](https://jira.hl7.org/browse/FHIR-50881) : Change Request |
| Title | Create CodeSystem and ValueSet to replace Ex-DiagnosisType |
| Work Group | Financial Mgmt |
| Status | Medium — Resolved - change required |
| Resolution | **Persuasive with Modification** |
| Labels | FM-R5+_ToBeApplied |
| Specification | FHIR Core (FHIR) |
| Reporter | Mark Scrimshire |
| Assignee | Unassigned |
| Created | 2025-05-14 |
| Updated | 2025-09-17 |
| Resolved | 2025-05-14 |

**Resolution text (verbatim):**

> Create new DiagnosisType CodeSystem.
>
> Inherit existing codes from Ex-DiagnosisType, except for "Self".
>
> Add PatientReported, Other and Secondary.
>
> Modify Claim and ExplanationOfBenefit .DiagnosisType to bind them to
> the new ValueSet.
>
> Binding to be set to Extensible.

Cross-references returned 0 hits, so the in-scope repos were inferred
from the resolution text (THO/UTG for the new CodeSystem; HL7/fhir for
the resource binding changes).
