# Implementation Plan: FHIR-41765 — HALTED (missing repo briefing)

| | |
|-|-|
| Ticket | [FHIR-41765](https://jira.hl7.org/browse/FHIR-41765) : Change Request |
| Work Group | Financial Mgmt |
| Status | Medium — Resolved - change required |
| Labels | FM-R5+_ToBeApplied |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter | Lloyd McKenzie |
| Assignee | Unassigned |
| Created | 2023-08-15 |
| Updated | 2025-01-27 |
| Resolved | 2025-01-27 |

## Halt Note

Per the `ticket-plan` skill, Step 3 requires a current per-repo briefing
(`cache/github/repos/<owner>_<name>/repo-analysis/briefing.md` plus
`meta.json`) for every in-scope repository. The required briefing is
**missing** for the primary in-scope repo, so this plan cannot proceed
without fabricating repo facts.

### In-scope repositories (from Steps 1–2)

- **HL7/UTG** — source of truth for the target artifact
  `CodeSystem-subscriber-relationship` (the ticket's resolution is to
  replace the Copyright on this CodeSystem with the standard HL7
  International Copyright). The artifact lives at:
  - `input/sourceOfTruth/fhir/codeSystems/CodeSystem-subscriber-relationship.xml`
  - `input/sourceOfTruth/fhir/valueSets/ValueSet-subscriber-relationship.xml` (associated ValueSet)
- **HL7/fhir** (secondary, possibly in scope) — carries a derived
  `source/coverage/valueset-subscriber-relationship.xml` pointing at the
  UTG-owned CodeSystem. Whether the core spec tree also needs a sync
  depends on UTG → FhirCore propagation rules, which are documented in
  the UTG briefing.

### Briefing status

| Repo | `repo-analysis/briefing.md` | `repo-analysis/meta.json` | Status |
|------|-----------------------------|---------------------------|--------|
| `HL7/UTG` | **missing** | **missing** | ❌ Halt — required for this ticket |
| `HL7/fhir` | present | present | ✅ Available |

### Action required

Please run the `repo-analysis` skill for `HL7/UTG` before resuming this
plan:

> `repo-analysis HL7/UTG`

Once `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` and
`meta.json` exist (and `meta.json` matches the current clone HEAD and
the current playbook SHA), re-run `ticket-plan FHIR-41765` and the full
plan — grounded in the UTG briefing's Authoring Roots, Artifact Map,
Change Recipes, and Gotchas — will be produced.

## Resolution Summary (for context only)

The ValueSet / CodeSystem `subscriber-relationship` has a `copyright`
element whose text reads "This is an extensible set." The reporter
objects that this is not copyright text and conflates a binding-strength
concept with the CodeSystem definition. The approved resolution is:

> Replace the Copyright with the standard HL7 International Copyright.

No code or content changes have been made as part of this halt note.

## Data Provenance

- Ticket fetched via `fhir-augury-cli` `get source=jira id=FHIR-41765`
  (orchestrator `http://localhost:5150`).
- Cross-references via `cross-referenced value=FHIR-41765` — one Zulip
  thread (`[terminology] Copyright for HL7-defined terminologies`,
  message id `385146223`); no GitHub cross-references.
- Keywords via `keywords source=jira id=FHIR-41765`.
- In-scope repo identified from the resolution text + the URL cited in
  the description (`terminology.hl7.org/5.0.0/CodeSystem-subscriber-relationship.xml.html`)
  plus a clone-tree check confirming
  `HL7/UTG:input/sourceOfTruth/fhir/codeSystems/CodeSystem-subscriber-relationship.xml`
  is the source-of-truth artifact.
