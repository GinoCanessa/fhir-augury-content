# Implementation Plan: FHIR-49887

**Status:** Halted — required repo briefing missing.

## Halt Reason

Per the `ticket-plan` skill (Step 3), a current per-repo briefing must exist
under `cache/github/repos/<owner>_<name>/repo-analysis/` for every in-scope
repository before a plan can be produced. The following required briefing is
**missing**:

- **`HL7/UTG`** — no `cache/github/repos/HL7_UTG/repo-analysis/briefing.md`
  and no `meta.json` present (the `repo-analysis` directory itself does not
  exist).

`HL7/UTG` is in scope for this ticket because the canonical CodeSystem
referenced by the ticket (`http://terminology.hl7.org/CodeSystem/processpriority`)
lives in UTG (`input/sourceOfTruth/fhir/codeSystems/CodeSystem-processpriority.xml`),
and a parallel `ValueSet-process-priority.xml` exists alongside it in
`input/sourceOfTruth/fhir/valueSets/`. The ticket title explicitly names both
`CodeSystem` and `ValueSet`, so the change cannot be scoped to `HL7/fhir`
alone.

The `HL7/fhir` briefing was checked and is fresh (`playbook_sha`
`9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4`, `clone_head_sha`
`db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` — both match the live values).

## Requested Action

Please run the `repo-analysis` skill for `HL7/UTG` and re-dispatch this
ticket once the briefing has been produced:

> `repo-analysis HL7/UTG`

## Ticket Snapshot (for reference)

| | |
|-|-|
| Ticket | [FHIR-49887](https://jira.hl7.org/browse/FHIR-49887) : Change Request |
| Title | Remove experimental flag on CodeSystem/ValueSet process-priority |
| Work Group | Financial Mgmt |
| Status | Medium · Resolved - change required |
| Resolution | Persuasive — "The CodeSystem and valueset are fit for purpose." |
| Specification | FHIR Core (FHIR) |
| Labels | FM-R5+_ToBeApplied |
| Reporter | Paul Knapp |
| Assignee | Unassigned |
| Created / Updated / Resolved | 2025-02-25 / 2025-02-25 / 2025-02-25 |

### Affected artifacts (preliminary, pending UTG briefing)

- `HL7/fhir` — `source/claim/valueset-process-priority.xml`
  - Already updated by FHIR-51521 (commit `77f2d360d1`, 2025-10-17): set
    `<experimental value="true"/>` → `<experimental value="false"/>`. The
    file currently shows `experimental=false` and `status=active`. Plan
    should confirm whether FM intends to fully remove the `<experimental>`
    element rather than set it to `false`.
- `HL7/UTG` — `input/sourceOfTruth/fhir/codeSystems/CodeSystem-processpriority.xml`
  and `input/sourceOfTruth/fhir/valueSets/ValueSet-process-priority.xml`
  - Both files in the cached clone already show `<experimental value="false"/>`
    (committed 2024-04-26). Plan needs the UTG briefing to determine the
    correct authoring root, build/IG-publisher recipe, and any UTG-specific
    gotchas (e.g., THO publication cadence, `lastUpdated`/`version` bump
    rules) before final tasks can be written.

No cross-references for `FHIR-49887` were returned by
`cross-referenced` (total = 0), so repo scoping was inferred from the
ticket title, the FM work group, and the keyword set
(`processpriority`, `valueset`, `codesystem`, `claim`,
`claimresponse`, `explanationofbenefit`).
