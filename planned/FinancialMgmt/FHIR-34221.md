# Implementation Plan: FHIR-34221

**Status:** Halted — missing repo briefing.

## Ticket

- **Key:** [FHIR-34221](https://jira.hl7.org/browse/FHIR-34221) — Update Tooth Surface Codes CodeSystem and make a non-example system
- **Type:** Change Request
- **Work Group:** Financial Mgmt
- **Specification:** FHIR Core (FHIR)
- **Labels:** FM-THO
- **Resolution:** Persuasive with Modification
- **Reporter:** Corey Spears
- **Resolved:** 2022-05-12

## Halt Reason

Per the `ticket-plan` skill Step 3, a current per-repo briefing
(`cache/github/repos/<owner>_<name>/repo-analysis/briefing.md` +
`meta.json`) must exist for every in-scope repository before a plan can
be produced. This ticket's in-scope set requires **two** repositories,
and one of them has no cached briefing:

| Repo | Category | Briefing present? | Clone HEAD |
|------|----------|-------------------|------------|
| `HL7/fhir` | FhirCore | ✅ present and fresh (playbook + clone HEAD match `meta.json`) | `db79dcd` |
| `HL7/UTG` | Terminology (THO) | ❌ **missing** (`cache/github/repos/HL7_UTG/repo-analysis/` does not exist) | n/a |

### Why HL7/UTG is in scope

The ticket is labelled **FM-THO** and the resolution targets the
`http://terminology.hl7.org/CodeSystem/FDI-surface` **CodeSystem** —
the `terminology.hl7.org` canonical namespace is owned by UTG/THO, not
by the core FHIR spec. Confirmed by locating the artifact in the UTG
clone:

- `cache/github/repos/HL7_UTG/clone/input/sourceOfTruth/fhir/codeSystems/CodeSystem-FDI-surface.xml`
- `cache/github/repos/HL7_UTG/clone/input/sourceOfTruth/fhir/valueSets/ValueSet-surface.xml`

The resolution (remove multi-surface combination codes, add `F` for
Facial, change content-type from example to a standard CodeSystem)
mutates that UTG CodeSystem directly, so a plan cannot be written
without a trusted briefing for `HL7/UTG` (authoring roots, generated
areas, change recipes, build system, gotchas).

`HL7/fhir` also needs edits (the ValueSet binding on `Claim.item.subSite`
and `ExplanationOfBenefit.item.subSite`, plus the FM `valueset-surface.xml`
that composes from the CodeSystem), and its briefing is present and
fresh — those edits are not the blocker.

### Data gathered so far

- Ticket fetched via `fhir-augury-cli` (`get` / `jira` / `FHIR-34221`,
  incl. comments, content, snapshot) — resolution text captured.
- Cross-references: `cross-referenced FHIR-34221` returned **0 hits**
  (no PRs, issues, or Zulip threads linked).
- Keywords: top BM25 terms are `surface`, `facial`, `tooth`, `labial`,
  `surfacecode`, `codesystem`, `subsite`, `combination` — consistent
  with a CodeSystem + binding change in the oral-health / Claim area.
- `HL7/fhir` clone search: no `codesystem-surface*` or `codesystem-FDI*`
  file exists under `source/` (confirming the CodeSystem is not
  authored in core), and `source/claim/valueset-surface.xml` composes
  from `http://terminology.hl7.org/CodeSystem/FDI-surface`.

Transient artifacts from this run live under
`C:\ai\git\fhir-augury\temp\plan\FHIR-34221-*.json` and are left in
place for the orchestrator.

## Requested Action

Please run the `repo-analysis` skill for **`HL7/UTG`** (fresh or
`if-stale`), then re-dispatch `ticket-plan` for FHIR-34221. Once
`cache/github/repos/HL7_UTG/repo-analysis/briefing.md` and `meta.json`
exist and match the current clone HEAD + playbook SHA, the plan will be
able to ground concrete file paths and change recipes for the
CodeSystem edits (remove combination codes, add `F` Facial, flip
content from example to standard) in UTG, alongside the binding /
ValueSet follow-ups in `HL7/fhir`.

No implementation tasks are being proposed in this document, to avoid
fabricating UTG repo facts (authoring roots, generated areas, and
change recipes) that the skill requires to be sourced from a saved
briefing.
