# Implementation Plan: FHIR-55210 — HALTED

**Ticket:** [FHIR-55210](https://jira.hl7.org/browse/FHIR-55210) — *Out-of-scope resource Contract has not been removed from ballot*
**Work Group:** Financial Mgmt
**Specification:** FHIR Core (FHIR)
**Resolution:** Persuasive

## Halt reason

Per the `ticket-plan` skill, Step 3 requires a current `repo-analysis`
briefing for every in-scope repository before an implementation plan may
be drafted. The in-scope repo for this ticket is **`HL7/fhir`** (FhirCore),
and its saved briefing is **stale**:

- `cache/github/repos/HL7_fhir/repo-analysis/meta.json`
  - `clone_head_sha`: `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` (matches current clone HEAD ✅)
  - `playbook_sha`: `9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4`
  - current source-controlled playbook `.github/skills/repo-analysis/categories/fhir-core.md` @ `67043498b4b3ef0c4990b7705c85aa0dbe6fbc7a` ❌

Per the `repo-analysis` skill staleness rules, a playbook-SHA mismatch
means the briefing is stale. The `ticket-plan` skill explicitly forbids
proceeding with partial repo context or fabricating repo facts, so this
run has been halted.

## Required action

Re-run the `repo-analysis` skill for `HL7/fhir` so the briefing is
regenerated against the current playbook:

> `repo-analysis HL7/fhir if-stale`

Once `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` and
`meta.json` have been refreshed, re-invoke `ticket-plan FHIR-55210`.

## Ticket data already gathered (for the re-run)

- **Status:** Resolved - change required (Persuasive)
- **Priority:** Highest
- **Reporter:** Lloyd McKenzie
- **Labels:** `FM-R5+_ToBeApplied`
- **Description (imported as comment 176):** *"The work group had previously
  agreed with the FMG that the Contract resource would be moved to an
  implementation guide, however this has not yet occurred. It needs to
  before the next ballot."*
- **Comments:** none
- **Cross-references:** none (`cross-referenced` returned 0 hits)
- **Top keywords:** `contract` (fhir_path), `ballot`, `scope`, `remove`,
  `move`, `implementation`, `guide`, `resource`, `fmg`, `FM-R5+_ToBeApplied`

Expected scope (for context only — not a substitute for the briefing):
removal of the `Contract` resource from the HL7/fhir core ballot (source
files, build registry, navigation, examples, cross-references) in line
with the prior FM ↔ FMG agreement to move it to a separate IG.
