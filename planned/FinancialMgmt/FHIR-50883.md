# Implementation Plan: FHIR-50883

| | |
|-|-|
| Ticket | [FHIR-50883](https://jira.hl7.org/browse/FHIR-50883) : Change Request |
| Work Group | Financial Mgmt |
| Status | Medium Resolved - change required |
| Labels | FM-R5+_ToBeApplied |
| Specification | FHIR Core (FHIR) |
| Reporter | Mark Scrimshire |
| Assignee | Unassigned |
| Created | 2025-05-14 |
| Updated | 2025-05-14 |
| Resolved | 2025-05-14 |
| Resolution | Persuasive |

---

## Halt: Required Per-Repo Briefing Missing

Planning has been **halted at Step 3** of the `ticket-plan` skill because a
required per-repo briefing is missing. Per the skill's rules, the agent must
not fabricate repo facts (authoring roots, paths, recipes) when a briefing is
unavailable.

### Resolution (for context only)

> Create new permanent CodeSystem in THO. Copy values from R5
> Ex-DiagnosisOnAdmission CodeSystem and change Binding to Extensible.

### In-Scope Repositories Identified

Inferred from the resolution wording ("THO" = Terminology.HL7.Org / UTG) and
the ticket's specification metadata (`FHIR Core (FHIR)`):

1. **HL7/UTG** (`Utg` category) — host the new permanent CodeSystem replacing
   the experimental `Ex-DiagnosisOnAdmission` CodeSystem.
2. **HL7/fhir** (`FhirCore` category) — retire / redirect the experimental
   `Ex-DiagnosisOnAdmission` CodeSystem currently embedded in the FHIR Core
   build and rebind the affected element(s) (e.g.
   `ExplanationOfBenefit.diagnosis.onAdmission`,
   `Claim.diagnosis.onAdmission`) to the new permanent THO CodeSystem with
   binding strength `extensible`.

### Briefing Status

| Repository | `briefing.md` | `meta.json` | Status |
|------------|---------------|-------------|--------|
| `HL7/fhir` | present | present | OK |
| `HL7/UTG`  | **missing** | **missing** | **Blocking** |

Checked paths:

- `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` — exists
- `cache/github/repos/HL7_fhir/repo-analysis/meta.json` — exists
- `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` — **missing**
- `cache/github/repos/HL7_UTG/repo-analysis/meta.json` — **missing**

The `HL7_UTG` clone is present at
`cache/github/repos/HL7_UTG/clone/` (with `input/`, `framework/`,
`pages/`, etc.), but no `repo-analysis/` directory has been generated.

### Action Required Before Resuming

Run the `repo-analysis` skill against `HL7/UTG` so the authoring roots,
ticket-relevant paths (CodeSystem source location), recommended change
recipes, and gotchas are captured:

> Please run `repo-analysis HL7/UTG` (or `repo-analysis HL7/UTG if-stale`)
> and let me know when the briefing has been written. After that, re-run
> `ticket-plan FHIR-50883` to produce the full implementation plan grounded
> in both the `HL7/fhir` and `HL7/UTG` briefings.

### Cross-References

`cross-referenced FHIR-50883` returned **0 hits** — there are currently no
known GitHub PRs/commits, related Jira tickets, or Zulip threads referencing
this ticket key in the augury index. Any related work (e.g., the THO
CodeSystem PR) will need to be discovered after the UTG briefing is in place.

### Keywords (Top, for routing only)

`diagnosisonadmission`, `codesystem` (fhir_path), `explanationofbenefit`
(fhir_path), `claim` (fhir_path), `extensible`, `permanent`, `r5`,
`tobeapplied`.

---

*Halted by ticket-plan at Step 3 (briefing check). No implementation plan
authored — repo facts for `HL7/UTG` would have to be fabricated to proceed.*
