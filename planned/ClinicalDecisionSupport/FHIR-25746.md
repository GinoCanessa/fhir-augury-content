# Implementation Plan: FHIR-25746 — HALTED

**Ticket:** [FHIR-25746](https://jira.hl7.org/browse/FHIR-25746) — *ActivityDefinition.Topic valueset should categorize the content of the activity*
**Work Group:** Clinical Decision Support
**Specification:** FHIR Core (FHIR)
**Resolution:** Persuasive
**Labels:** UTG

## Halt Note

Planning was halted at Step 3 of the `ticket-plan` skill because a required
per-repo briefing is missing. Per the skill's Step 3 guard, this plan will
not be completed with partial repo context, and no repo facts will be
fabricated.

### In-scope repositories (inferred)

Derived from the ticket metadata (Specification = FHIR Core; label `UTG`;
applied resolution text) and from the most recent comment on the ticket
(bryn.rhodes, 2023-03-12):

> The definition topic code system resides in terminology.hl7.org now, so
> this change will need a UTG ticket.

Two repositories are therefore in scope:

| Repository | Category | Briefing present? |
|------------|----------|-------------------|
| `HL7/fhir` | FhirCore | ✅ `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` (analyzed 2026-04-21, clone HEAD `db79dcdf`) |
| `HL7/UTG`  | Utg      | ❌ **missing** — `cache/github/repos/HL7_UTG/repo-analysis/` does not exist |

(Note: the `cross-referenced` query returned only FHIR-artifact targets —
`ActivityDefinition.topic`, `PlanDefinition.topic` — and no GitHub
PR/issue/commit cross-references, so the in-scope repo set is inferred
from metadata + resolution text rather than GitHub cross-references.)

### Requested action

Please run the `repo-analysis` skill for `HL7/UTG` and then re-dispatch
`ticket-plan` for `FHIR-25746`:

```
repo-analysis HL7/UTG if-stale
```

Once `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` and
`meta.json` exist (and `HL7/fhir`'s briefing is still current), the plan
can be produced end-to-end. The FHIR Core side is expected to be
minimal — at most an update to the example ValueSet
`ValueSet/definition-topic` references — with the substantive code
additions (new concept codes such as `diabetes-screening`,
`zika-screening`, `mammography-screening`, `colonoscopy-screening`,
`fecal-occult-blood-screening`, `pama-auc-evaluation`) landing in the
UTG repository where the `definition-topic` CodeSystem now lives.

### Data gathered so far (for the re-run)

- Ticket snapshot: `temp/plan/FHIR-25746.ticket.json`
- Cross-references: `temp/plan/FHIR-25746.xref.json` (2 FHIR targets, 0 GitHub)
- Keywords: `temp/plan/FHIR-25746.kw.json` (top: `activitydefinition.topic`, `definitiontopic`, `plandefinition.topic`, `valueset`)
