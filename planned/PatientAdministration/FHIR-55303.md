# Implementation Plan: FHIR-55303

**Status:** Halted — required repo briefing missing.

## Required briefing missing

This ticket's resolution migrates a FHIR Core code system and value set
(`http://hl7.org/fhir/organization-role` →
`http://terminology.hl7.org/CodeSystem/organization-affiliation-role`) into
**HL7/UTG (THO)**. UTG is therefore an in-scope repository for this plan,
but no per-repo briefing is cached for it:

- Missing: `cache/github/repos/HL7_UTG/repo-analysis/briefing.md`
- Missing: `cache/github/repos/HL7_UTG/repo-analysis/meta.json`
- Cache contains only `cache/github/repos/HL7_UTG/clone/` (no `repo-analysis/` directory).

Per the `ticket-plan` skill (Step 3), this skill is data-only with respect
to `repo-analysis` and must stop when any required briefing is missing
rather than fabricate UTG layout / recipe facts.

> Please run the `repo-analysis` skill for **HL7/UTG** and let me know
> when the briefing is ready, then re-run `ticket-plan FHIR-55303`.

The HL7/fhir briefing is fresh
(`playbook_sha=9c2e8e5b…`, `clone_head_sha=db79dcdfe1…`) and is not the
blocker.

---

## Ticket snapshot (gathered before halt)

| | |
|-|-|
| Ticket | [FHIR-55303](https://jira.hl7.org/browse/FHIR-55303) : Change Request |
| Work Group | Patient Administration |
| Status | Highest — Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter | Lloyd McKenzie |
| Assignee | Unassigned |
| Created | 2026-01-20 |
| Updated | 2026-02-10 |
| Resolved | 2026-02-10 |
| Related FHIR artifacts | OrganizationAffiliation.code, OrganizationAffiliation.specialty |
| Related Jira tickets | FHIR-55312 (PractitionerRole companion — `.specialty` move already decided there) |

### Resolution (verbatim, lightly de-HTMLed)

- **OrganizationAffiliation.specialty** — will move to THO (already decided
  via FHIR-55312, the PractitionerRole companion ticket).
- **OrganizationAffiliation.code** — NDH already has a THO code system
  `http://terminology.hl7.org/CodeSystem/organization-affiliation-role`.
  Move the existing codes from `http://hl7.org/fhir/organization-role` into
  the existing THO code system.
- For the **value set**, move it into THO as well: the code system is
  mature/stable enough for THO, and the value set CLD is (or will be) the
  full code system, which suggests the value set is "stable enough" for
  THO even though the binding strength on `OrganizationAffiliation.code`
  is only `example`.

### Likely scope once UTG briefing is available

- **HL7/fhir (FhirCore)** — retire the FHIR-Core copies of the
  `organization-role` CodeSystem and ValueSet (or convert to THO
  forwarders), and re-bind `OrganizationAffiliation.code` to the THO
  ValueSet URL. (HL7/fhir briefing is loaded and current.)
- **HL7/UTG (Utg)** — extend the existing
  `organization-affiliation-role` THO CodeSystem with the codes currently
  defined in FHIR-Core's `organization-role`; publish the THO ValueSet
  that `OrganizationAffiliation.code` will bind to. Specific UTG
  authoring root(s), recipe(s), and gotchas cannot be cited until the
  UTG briefing is generated.

### Cross-references (from `cross-referenced FHIR-55303`)

- FHIR artifact mention: `OrganizationAffiliation.code`
- FHIR artifact mention: `OrganizationAffiliation.specialty`
- No GitHub PRs/issues/commits cross-reference this ticket yet.

### Top keywords

`organizationaffiliation`, `organizationaffiliation.code`,
`organizationaffiliation.specialty`, `organizationaffiliationrole`,
`organizationrole`, `migration`, `move`, `system`, `practitionerrole`
(companion ticket FHIR-55312).

---

**Action required:** run `repo-analysis HL7/UTG` (fresh), then re-invoke
`ticket-plan FHIR-55303` to produce the full Repo Context, Impact
Analysis, and Step-by-Step Tasks sections grounded in the UTG briefing.
