# Implementation Plan: FHIR-55309

**Status:** Halted — required repo briefing missing.

The resolution for [FHIR-55309](https://jira.hl7.org/browse/FHIR-55309)
("Potentially re-useable THO-candidate value sets in resource Practitioner")
is **Persuasive** with the following resolution description:

> In alignment with the Organization use of QualificationStatus, the
> qualification.status value-set is being moved to THO. qualification.code is
> already in THO and language is addressed in a separate tracker.

Implementing this requires changes in two repositories:

1. **HL7/fhir** — rebind `Practitioner.qualification.status` to the THO-hosted
   ValueSet and remove the FHIR-Core copies of the ValueSet/CodeSystem.
2. **HL7/UTG** — add (or accept the migration of) the `QualificationStatus`
   CodeSystem and `qualification-status` ValueSet so they are hosted at
   terminology.hl7.org.

Per the `ticket-plan` skill (Step 3), every in-scope repository must have a
current per-repo briefing under
`cache/github/repos/<owner>_<name>/repo-analysis/`.

## Briefing Status

| Repository | Briefing | Note |
|------------|----------|------|
| HL7/fhir   | ✅ Fresh (`clone_head_sha` `db79dcdf…`, `playbook_sha` `9c2e8e5b…`) | OK |
| **HL7/UTG** | ❌ **Missing** (`cache/github/repos/HL7_UTG/repo-analysis/` does not exist) | Halt |

## Action Required

Please run the `repo-analysis` skill for `HL7/UTG` (e.g., `repo-analysis
HL7/UTG if-stale`) and re-dispatch this ticket. The full implementation plan
cannot be produced without grounded UTG repo context — the THO migration
itself lives entirely in that repository.

## Data Gathered (for re-use after briefing is generated)

- **Ticket metadata:** Patient Administration · FHIR Core · Change Request ·
  Highest priority · Resolution `Persuasive` · Reporter Lloyd McKenzie ·
  Created 2026-01-19 · Updated/Resolved 2026-02-17.
- **Cross-references:** 1 FHIR-path mention — `Practitioner.qualification.status`.
- **Top keywords:** `qualificationstatus`, `qualification`, `migration`,
  `practitioner.qualification.status`, `practitioner`, `codesystem`,
  `organization`.
- **Companion ticket noted in comments** (brian.postlethwaite, 2026-01-26):
  "These 2 tickets refer to the same valueset/codesystem from different
  resources." — likely the parallel Organization-side QualificationStatus
  migration ticket; should be linked when the UTG briefing is available.
