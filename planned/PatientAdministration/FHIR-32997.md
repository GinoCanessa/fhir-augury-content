# Implementation Plan: FHIR-32997

**Halted: required repo briefing missing.**

The resolution for this ticket (`Persuasive with Modification`) directs HL7
to *"propose to UTG to create a union of the v2 Table0912 representing persons
for the practitioner role … using v2 codes where FHIR does not have any yet"*
and to map the v2 code `MDIR` ("Medical Director") via
`https://terminology.hl7.org/ValueSet-v2-0912.html`. The substantive work
therefore lives in the **HL7/UTG** repository (CodeSystem / ValueSet
authoring), with a possible follow-up binding adjustment in **HL7/fhir** to
the example `practitioner-role` ValueSet.

A current `repo-analysis` briefing for **`HL7/UTG`** is **missing**
(`cache/github/repos/HL7_UTG/repo-analysis/` does not exist, although the
clone is present). Per the `ticket-plan` skill (Step 3), this skill is
data-only with respect to `repo-analysis` and must not proceed with partial
repo context or invent UTG layout / recipes.

**Action required:** run

> `repo-analysis HL7/UTG if-stale`

(or a refresh if you prefer) and re-dispatch this ticket. The HL7/fhir
briefing at `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` is fresh
(`clone_head_sha db79dcdf…`, `playbook_sha 9c2e8e5b…`) and does not need
re-running.

---

## Ticket Snapshot (gathered before halt)

| | |
|-|-|
| Ticket | [FHIR-32997](https://jira.hl7.org/browse/FHIR-32997) : Change Request |
| Title | Add Laboratory Medical Director as practitioner role |
| Work Group | Patient Administration |
| Status | Medium · Resolved - change required |
| Labels | Terminology-HTA |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter | Hans Buitendijk |
| Assignee | Hans Buitendijk |
| Created | 2021-06-30 |
| Updated | 2025-10-31 |
| Resolved | 2021-09-24 |

**Resolution text:**
> Motion to propose to UTG to create a union of the v2 Table0912 representing
> persons for the practitioner role
> http://www.hl7.org/fhir/valueset-practitioner-role.html, preferable using
> v2 codes where FHIR does not have any yet, while v2-FHIR then assigns
> medical director to v2 code "MDIR" and
> "https://terminology.hl7.org/ValueSet-v2-0912.html".

**Cross-references:** `cross-referenced FHIR-32997` returned 0 hits.

**Top keywords (Jira):** director, table0912, mdir, v2, medical, role,
laboratory, practitioner (fhir_path), practitionerrole (fhir_path), utg,
terminology, lab.

**Inferred in-scope repos:**

- **HL7/UTG** (Utg) — primary; needs union ValueSet authoring and confirmation
  that v2-0912 already publishes `MDIR = Medical Director` (or that the code
  is added there). **Briefing missing — halt.**
- **HL7/fhir** (FhirCore) — secondary; the example-bound
  `valueset-practitioner-role` may be re-authored to compose
  the v2-0912 ValueSet (or left as-is, since the binding is `example`).
  Briefing fresh, but ticket-relevant paths in the cached briefing are
  scoped to a different ticket; would be re-derived from `source/valueset/`
  and `source/practitioner/` once HL7/UTG context is available.

No further analysis was performed because Step 3 requires both briefings
before producing an actionable plan.
