# Implementation Plan: FHIR-49709 — HALTED (stale repo briefing)

| | |
|-|-|
| Ticket | [FHIR-49709](https://jira.hl7.org/browse/FHIR-49709) : Change Request |
| Title | Improve benefit definitions in InsurancePlan |
| Work Group | Patient Administration |
| Status | Medium — Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter | Cooper Thompson |
| Assignee | Unassigned |
| Labels | AdditionalResources |
| Created | 2025-01-30 |
| Updated | 2025-10-28 |
| Resolved | 2025-03-11 |

## Halt Reason

Per the `ticket-plan` skill Step 3, for every in-scope repository the agent
must read `cache/github/repos/<owner>_<name>/repo-analysis/briefing.md`
and `meta.json` and verify freshness (clone HEAD **and** playbook SHA both
match current). If a briefing is missing or stale, the skill must STOP and
ask the user to run `repo-analysis` rather than fabricate repo facts.

### In-scope repository

The ticket targets `FHIR Core (FHIR)`. There are **no GitHub
cross-references** in Augury for FHIR-49709; the only referenced artifacts
are FHIR elements (`InsurancePlan.specificCost.benefit.type`,
`CoverageEligibilityResponse.item.*`). Per Step 2, this defaults to
`HL7/fhir`.

### Staleness check for `HL7/fhir`

Path: `cache/github/repos/HL7_fhir/repo-analysis/`

| Check | Expected (current) | meta.json | Match |
|-|-|-|-|
| `clone_head_sha` | `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` | `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` | ✅ |
| `playbook_sha` (`.github/skills/repo-analysis/categories/fhir-core.md`) | `67043498b4b3ef0c4990b7705c85aa0dbe6fbc7a` | `9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4` | ❌ |

The playbook SHA does not match, so the briefing is **stale** per the
`repo-analysis` skill's freshness rule ("Any mismatch → stale").

## Requested Action

> Please run `repo-analysis HL7/fhir if-stale` (or `refresh`) to regenerate
> `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` + `meta.json`
> against the current `fhir-core.md` playbook, then re-dispatch
> `ticket-plan FHIR-49709`.

No implementation plan will be produced until the briefing is refreshed,
to avoid grounding the plan in out-of-date repo facts.

## Data gathered so far (for reference)

- **Resolution description (Jira):** "We will align with
  CoverageEligibilityResponse as outlined above, with the addition of
  modifier. These will all be optional with the same datatypes as found
  on CoverageEligibilityResponse."
- **FHIR artifacts mentioned (Augury cross-refs):**
  `InsurancePlan.specificCost.benefit.type`, `CoverageEligibilityResponse.item`,
  `CoverageEligibilityResponse.item.category`,
  `CoverageEligibilityResponse.item.productOrService`,
  `CoverageEligibilityResponse.item.excluded`,
  `CoverageEligibilityResponse.item.name`,
  `CoverageEligibilityResponse.item.description`,
  `CoverageEligibilityResponse.item.network`,
  `CoverageEligibilityResponse.item.unit`,
  `CoverageEligibilityResponse.item.term`.
- **GitHub cross-references:** none.
- **Zulip cross-references:** 1 ("[patient administration WG] Conference call").
