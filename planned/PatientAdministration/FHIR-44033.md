# Implementation Plan: FHIR-44033

| | |
|-|-|
| Ticket | [FHIR-44033](https://jira.hl7.org/browse/FHIR-44033) : Change Request |
| Work Group | Patient Administration |
| Status | Medium Resolved - change required |
| Labels | AdditionalResources |
| Specification | FHIR Core (FHIR) |
| Related Artifacts | (none listed) |
| Related Pages | PA Confluence: `$merge for Encounter` (https://confluence.hl7.org/display/PA/%24merge+for+Encounter) |
| Related URLs | https://hl7.org/fhir/6.0.0-ballot1/patient-operation-merge.html#8.1.18.3.4.4.3 |
| Related Sections | (none) |
| Reporter | Reinhard Egelkraut |
| Assignee | Reinhard Egelkraut |
| In-Person | (none) |
| Created | 2024-01-22 |
| Updated | 2025-10-28 |
| Resolved | 2025-01-29 |

---

## ⛔ Halt: Required Repo Briefing Missing

This plan cannot be produced end-to-end because a required per-repo briefing is
missing. Per the `ticket-plan` skill (Step 3) and the orchestrator's critical
rules, the run stops here rather than fabricate repo facts.

### Resolution Summary

- **Resolution:** Persuasive.
- **Resolution description:** *"This new operation will be added as defined in
  the PA project page linked."*
- **Comments:** Brian Postlethwaite (2025-01-29) — *"This issue is now being
  addressed for R6"*. Earlier PA January 2024 WGM minutes: PA has an active
  project for an encounter-level `$merge`, drafting is happening on the PA
  Confluence page and Zulip.
- **What must be implemented:** A new operation `Encounter-merge` (analogous to
  `Patient-merge`), defined per the PA Confluence project page
  `https://confluence.hl7.org/display/PA/%24merge+for+Encounter`.

### Why This Halts

The authoritative home for the analogous `Patient-merge` operation has been
moved out of FHIR Core into the **Patient Administration admin-incubator** IG.
This is stated directly in the core repo's own Patient narrative:

- `HL7/fhir` → `source/patient/patient-notes.xml` (lines 34 and 48) links to
  `https://build.fhir.org/ig/HL7/admin-incubator/branches/main/en/OperationDefinition-Patient-merge.html`
  as the canonical location of the Patient `$merge` operation.
- `HL7/fhir` → `source/patient/` no longer contains an
  `operationdefinition-Patient-merge.xml`; only `operation-Patient-merge-notes.xml`
  remains as supporting prose. The R6 ballot1 `patient-operation-merge.html`
  page referenced by the ticket reporter is generated from the admin-incubator
  IG, not from FHIR Core.
- `list-Patient-operations.xml` only lists `Patient-match` and `Patient-purge`
  — `Patient-merge` is not in Core.

By direct analogy, the new **`Encounter-merge`** operation produced by this
ticket is expected to be authored in **`HL7/admin-incubator`**, not in
`HL7/fhir`. `HL7/admin-incubator` is therefore in-scope for this plan.

### Briefing Status

Checked `cache/github/repos/<owner>_<name>/repo-analysis/briefing.md` +
`meta.json` for each potentially in-scope repo:

| Repo | Briefing | Meta | Clone HEAD match | Playbook SHA match | Status |
|---|---|---|---|---|---|
| `HL7/fhir` (FhirCore) | present | present | ✅ `db79dcd` | ✅ `9c2e8e5` | **Current** (scoped to FHIR-42980, facts section still valid) |
| `HL7/admin-incubator` (PA admin-incubator IG) | **missing** | **missing** | n/a | n/a | **HALT — no briefing** |

The `HL7/admin-incubator` clone exists under
`cache/github/repos/HL7_admin-incubator/clone/` (sushi-config.yaml, ig.ini,
input/ present — a sushi/IG-Publisher IG), but no
`cache/github/repos/HL7_admin-incubator/repo-analysis/briefing.md` has been
generated. Without it, the authoring roots (FSH vs XML), existing
`Patient-merge` file paths, recipes for adding a new OperationDefinition, and
"do not edit" areas for this IG are unknown — and must not be guessed.

### What's Required to Resume

Run the `repo-analysis` skill for `HL7/admin-incubator` and (optionally) refresh
the core briefing to re-scope its Ticket-Relevant Paths away from FHIR-42980:

```text
repo-analysis HL7/admin-incubator refresh
repo-analysis HL7/fhir if-stale FHIR-44033
```

Once both briefings exist and pass the staleness check documented in the
`repo-analysis` skill (clone HEAD + playbook SHA both match), re-dispatch this
ticket to `ticket-plan`.

### What's Known So Far (Not Yet a Plan)

Recorded here only as context for the resume, not as an implementation plan:

- **Primary deliverable:** New `OperationDefinition/Encounter-merge` modelled on
  `Patient-merge`, targeting FHIR R6. Exact path is determined by
  admin-incubator's authoring conventions (FSH under `input/fsh/` vs native XML)
  — unknown until the briefing is loaded.
- **Likely supporting changes in `HL7/admin-incubator`:** narrative page for the
  operation; examples for request/response/provenance analogous to Core's
  `source/patient/$merge-request.txt`, `$merge-response.txt`,
  `$merge-provenance.txt`; IG list/ImplementationGuide entries.
- **Likely touch-points in `HL7/fhir`** (FhirCore briefing, authoring root
  `source/` only, see `repo-analysis/briefing.md` @ clone `db79dcd`):
  - `source/encounter/encounter-notes.xml` (or `encounter-introduction.xml`) —
    add a narrative link to the admin-incubator `Encounter-merge` operation,
    mirroring the pattern used in `source/patient/patient-notes.xml` lines 34
    and 48.
  - `source/encounter/list-Encounter-operations.xml` — currently empty of
    operation entries; may not need a Core entry if `Encounter-merge` lives in
    admin-incubator, but a cross-link is worth evaluating.
  - Do **not** add `operationdefinition-Encounter-merge.xml` under
    `source/encounter/` — that would contradict the established pattern of
    Patient `$merge` being hosted in admin-incubator.
- **Out of scope for this ticket (per PA Jan 2024 WGM comment):**
  de-duplication of encounter content merging (noted as a separate, open
  design question for both Patient and Encounter `$merge`).
- **Cross-references:** `cross-referenced` returned 0 hits for `FHIR-44033` —
  no open PR or companion ticket has been linked in the Augury index at the
  time of this run.

### Sources

- Ticket snapshot: `temp/plan/FHIR-44033-get.json` (fhir-augury-cli `get jira`)
- Cross-references: `temp/plan/FHIR-44033-xrefs.json` (0 hits)
- Keywords: `temp/plan/FHIR-44033-kw.json` (top: `$merge` (fhir_operation),
  `encounter` (fhir_path), `a42`, `a41`, `adt`)
- Core briefing: `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` @
  clone `db79dcd`, playbook `9c2e8e5`
- Patient $merge relocation evidence:
  `cache/github/repos/HL7_fhir/clone/source/patient/patient-notes.xml` lines
  31–48, and absence of `operationdefinition-Patient-merge.xml` under
  `cache/github/repos/HL7_fhir/clone/source/patient/`.
