# Implementation Plan: FHIR-55302 — HALTED

| | |
|-|-|
| Ticket | [FHIR-55302](https://jira.hl7.org/browse/FHIR-55302) : Change Request |
| Title | Potentially re-useable THO-candidate value sets in resource Organization |
| Work Group | Patient Administration |
| Status | Highest · Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter | Lloyd McKenzie |
| Assignee | Unassigned |
| Created | 2026-01-19 |
| Updated | 2026-02-17 |
| Resolved | 2026-02-17 |

**Resolution description:** "Will move value-sets for qualification.status and type to THO."

---

## Halt Reason — Missing Per-Repo Briefing

This ticket's resolution requires changes in **two** repositories:

1. **HL7/fhir** (FhirCore) — update the binding(s) on `Organization.qualification.status` (and per the resolution, `Organization.qualification.type`) so they point at the THO-hosted value sets, and remove the now-relocated local value set / code system definitions.
2. **HL7/UTG** (Utg) — receive (re-home) the migrated value sets and supporting code systems for `qualification-status` and the `qualification.type` binding so THO becomes the canonical owner.

Per the `ticket-plan` skill, every in-scope repository must have a current
`repo-analysis/briefing.md` + `meta.json` before the plan can be drafted.
The `ticket-plan` skill is data-only with respect to `repo-analysis` and
explicitly forbids fabricating repo facts when a briefing is missing.

| Repo | Briefing path | Present? | Notes |
|------|---------------|----------|-------|
| HL7/fhir | `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` | ✅ Yes | meta.json present, category `FhirCore`, clone HEAD `db79dcdf` (recorded under ticket key FHIR-42980). Staleness vs. current clone HEAD has not been verified — re-confirm during resumption. |
| HL7/UTG | `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` | ❌ **Missing** | Only `clone/` exists under `cache/github/repos/HL7_UTG/`; no `repo-analysis/` directory. |

Because `HL7/UTG` is the destination of the THO migration that this ticket
mandates, no portion of the implementation plan can be grounded without
its briefing (authoring root, value-set/code-system folder layout,
publishing/build expectations, recipes for relocating an externally-bound
value set into THO, gotchas around canonical URL changes, etc.).

## Action Required Before Resuming

Please run the `repo-analysis` skill for the missing repo (and refresh
the FhirCore briefing if its clone HEAD has moved):

```
repo-analysis HL7/UTG
repo-analysis HL7/fhir if-stale
```

Once both `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` (with a
matching `meta.json`) and a current `cache/github/repos/HL7_fhir/repo-analysis/briefing.md`
exist, re-invoke `ticket-plan FHIR-55302` and the plan will be produced
in full.

## Data Already Gathered (cached for resumption)

The following raw responses are saved under `temp/plan/` and can be
re-used on resumption without re-querying:

- `temp/plan/FHIR-55302-get.json` — full ticket (content, metadata, snapshot)
- `temp/plan/FHIR-55302-xref.json` — cross-references (1 hit: FHIR target `Organization.qualification.status`, no GitHub PRs/commits/issues link to this ticket)
- `temp/plan/FHIR-55302-kw.json` — top keywords (`qualification`, `migration`, `Organization.qualification.status`, `qualificationstatus`, `Organization`, `valueset`, `codesystem`, …)

### Key facts captured for the eventual plan

- The resolution is `Persuasive with Modification`, so an implementation
  is required (the resolution-type guard does **not** trip).
- The original ticket text scopes the discussion to `qualification-status`
  / `QualificationStatus` bound to `Organization.qualification.status`.
- The applied resolution **broadens** the change to also include the
  value set bound to `Organization.qualification.type` ("Will move
  value-sets for qualification.status and type to THO"). The plan must
  treat both bindings as in scope.
- Cross-reference search returned only the FHIR-path mention; no related
  Jira tickets, Zulip threads, GitHub PRs, commits, or issues currently
  link to FHIR-55302 in the cache.

---

*This is a halt-note produced by the `ticket-plan` skill (Step 3, missing
briefing). No fabricated repo facts are included. No further work was
performed on the implementation plan.*
