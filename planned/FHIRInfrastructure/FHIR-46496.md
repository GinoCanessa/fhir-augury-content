# Implementation Plan: FHIR-46496

**Status:** Halted — required per-repo briefing missing.

## Reason

Per the `ticket-plan` skill's Step 3, every repository in scope for the
ticket must have a current per-repo briefing under
`cache/github/repos/<owner>_<name>/repo-analysis/`. The HL7/fhir briefing
is fresh (clone HEAD `db79dcdfe1…`, playbook SHA `9c2e8e5b…` — both
match), but the briefing for `HL7/fhir-subscription-backport-ig` is
**missing**:

- `cache/github/repos/HL7_fhir-subscription-backport-ig/clone/` — present
- `cache/github/repos/HL7_fhir-subscription-backport-ig/repo-analysis/` — **does not exist**

This repo is in scope because the resolution work for FHIR-46496 is
being relocated to the Subscriptions Backport IG. The ticket's
2025-11-04 comment by ginocanessa states:

> In FHIR R6, the websocket details are moving to the backport IG. Will
> update this ticket as applied when the documentation is verified
> complete and correct there.

The companion ticket FHIR-39035 (also `MovingToGuide`, same author and
same wording) confirms the same destination.

## Action requested

Please run the `repo-analysis` skill for the missing repo, then resume
ticket-plan for FHIR-46496:

```text
repo-analysis HL7/fhir-subscription-backport-ig if-stale
```

Once `cache/github/repos/HL7_fhir-subscription-backport-ig/repo-analysis/briefing.md`
and `meta.json` exist (with `clone_head_sha` matching the current clone
HEAD and `playbook_sha` matching the active playbook), re-invoke
ticket-plan for FHIR-46496.

## Ticket snapshot (for context)

| | |
|-|-|
| Ticket | [FHIR-46496](https://jira.hl7.org/browse/FHIR-46496) — Change Request |
| Title | Additional Websocket clarity |
| Work Group | FHIR Infrastructure |
| Status | Medium · Resolved - change required |
| Resolution | Persuasive |
| Labels | MovingToGuide |
| Specification | FHIR Core (FHIR) |
| Reporter | Gino Canessa |
| Assignee | Gino Canessa |
| Created | 2024-07-29 |
| Updated | 2025-11-04 |
| Resolved | 2024-08-05 |

**Resolution description:** *Add detailed examples with literals in the
appropriate protocols for each of the channel types, including EBNF
notation for the expressions.*

**Latest comment (2025-11-04, ginocanessa):** *In FHIR R6, the websocket
details are moving to the backport IG. Will update this ticket as
applied when the documentation is verified complete and correct there.*

**Related ticket:** FHIR-39035 (Define the bind-with-token Message Used
in WebSockets Subscription Channel Type) — same `MovingToGuide` label,
same destination.
