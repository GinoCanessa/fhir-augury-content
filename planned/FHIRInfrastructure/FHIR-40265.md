# Implementation Plan: FHIR-40265

| | |
|-|-|
| Ticket | [FHIR-40265](https://jira.hl7.org/browse/FHIR-40265) : Change Request |
| Title | Enable content from detailed descriptions to be pulled into content |
| Work Group | FHIR Infrastructure |
| Status | Medium — Resolved - change required |
| Resolution | Persuasive with Modification |
| Specification | FHIR Core (FHIR) |
| Labels | Deferred, FHIR-Tools-IG |
| Reporter | Christopher Schaut |
| Assignee | Lloyd McKenzie |
| Created | 2023-01-18 |
| Updated | 2023-03-19 |
| Resolved | 2023-03-13 |

## Halt: Missing Repo Briefing

Per the `ticket-plan` skill, Step 3 requires a current per-repo briefing
(`briefing.md` + `meta.json`) under
`cache/github/repos/<owner>_<name>/repo-analysis/` for every in-scope
repository. Planning cannot proceed with fabricated repo facts.

### Resolution Summary (for orchestrator context only)

The resolution description states:

> We will update the ig-guidance IG to explain how to make this happen.

The ticket asks that IG authors be able to reference content from the
"detailed descriptions" of a StructureDefinition (e.g., Definition, Note)
so that the same text can be surfaced in narrative pagecontent without
duplication. The agreed disposition is **documentation-only**: add
guidance to the `ig-guidance` IG explaining how IG authors can achieve
this with existing IG Publisher tooling (see referee notes at
<https://hackmd.io/9t1EZ9PwTra0DqH4daoI4g?view>).

### In-Scope Repository (inferred)

From the resolution text and the cached clone layout, the single
in-scope repository is:

- **FHIR/ig-guidance** — clone present at
  `cache/github/repos/FHIR_ig-guidance/clone/` (IG authoring root
  appears to be `input/pagecontent/`).

No GitHub cross-references were returned by
`cross-referenced FHIR-40265`, so this is the only repo identified.

### Missing Artifacts

| Repo | `briefing.md` | `meta.json` |
|------|---------------|-------------|
| `FHIR/ig-guidance` | **missing** (`cache/github/repos/FHIR_ig-guidance/repo-analysis/briefing.md`) | **missing** (`cache/github/repos/FHIR_ig-guidance/repo-analysis/meta.json`) |

The `repo-analysis/` directory does not exist under
`cache/github/repos/FHIR_ig-guidance/`.

### Action Required

Please run the `repo-analysis` skill for `FHIR/ig-guidance` and rerun
`ticket-plan FHIR-40265` once the briefing is in place:

> Briefing for `FHIR/ig-guidance` is missing (no `repo-analysis/`
> directory under the cached repo). Please run
> `repo-analysis FHIR/ig-guidance` and let me know when it's ready.

Until then, no implementation plan is produced — per the skill's
"Trust the saved briefing" / "do not fabricate repo facts" rules.
