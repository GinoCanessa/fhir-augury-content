# Implementation Plan: FHIR-55163 — HALTED (Missing Repo Briefing)

**Ticket:** [FHIR-55163](https://jira.hl7.org/browse/FHIR-55163) — *Potentially re-useable THO-candidate value sets in resource Group*
**Resolution:** Persuasive
**Work Group:** FHIR Infrastructure
**Specification:** FHIR Core (FHIR)

## Why this plan was halted

Per the `ticket-plan` skill's Step 3 guard, planning stops when a required
per-repo briefing is missing or stale. This skill must not fabricate repo
facts.

The resolution explicitly requires moving four value sets and their
underlying code systems from FHIR Core to **THO (terminology.hl7.org)**,
which is maintained in **`HL7/UTG`**. That makes `HL7/UTG` an in-scope
repository for this ticket alongside `HL7/fhir`.

Briefing status for in-scope repos:

| Repository | Category | Briefing | Meta | Status |
|---|---|---|---|---|
| `HL7/fhir` | FhirCore | `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` | present (`clone_head_sha=db79dcdf`, matches current clone HEAD) | OK |
| `HL7/UTG` | Utg | `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` | **missing** | **BLOCKING** |

`cache/github/repos/HL7_UTG/repo-analysis/` does not exist — neither
`briefing.md` nor `meta.json` is present.

## Required action before re-running

Run the `repo-analysis` skill for `HL7/UTG`, then re-dispatch this
ticket:

```
repo-analysis HL7/UTG if-stale
```

Once `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` and
`meta.json` exist and are current, re-run `ticket-plan FHIR-55163` to
produce the full plan.

## Ticket context gathered so far (for re-run convenience)

- **Resolution type:** Persuasive (not a stub case — full plan required).
- **Resolution text (verbatim summary):** move the following value sets
  and their underlying code systems to THO, keeping existing URLs:
  - `valueset-group-code` (CodeSystem `GroupType`) — bound to `Group.code`
  - `valueset-definition-method` (CodeSystem `DefinitionMethod`) — bound to `Group.characteristic.method`
  - `valueset-characteristic-offset` (CodeSystem `CharacteristicOffset`) — bound to `Group.characteristic.offset`
  - `valueset-group-involvement-set` (CodeSystem `GroupInvolvement`) — bound to `Group.member.involvement`
- **FHIR cross-references:** `Group.code`, `Group.characteristic.method`,
  `Group.characteristic.offset`, `Group.member.involvement` (no GitHub
  PRs or issues linked).
- **Cached inputs in the working dir:**
  `temp/plan/FHIR-55163-get.json`, `temp/plan/FHIR-55163-xrefs.json`,
  `temp/plan/FHIR-55163-kw.json`.

No further planning has been attempted; doing so without the `HL7/UTG`
briefing would violate the skill's grounding rule.
