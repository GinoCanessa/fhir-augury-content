# Implementation Plan: FHIR-51476

| | |
|-|-|
| Ticket | [FHIR-51476](https://jira.hl7.org/browse/FHIR-51476) : Change Request |
| Work Group | FHIR Infrastructure |
| Status | Highest / Resolved - change required |
| Labels | — |
| Specification | FHIR Core (FHIR) |
| Related Artifacts | TestScript (resource), TestScript.metadata.capability, TestScript.metadata.capability.required, TestScript.metadata.capability.validated |
| Related Pages | testscript-definitions.html |
| Related URLs | https://hl7.org/fhir/R5/testscript-definitions.html#TestScript.metadata.capability.required, https://hl7.org/fhir/R5/conformance-rules.html#rule |
| Related Sections | TestScript.metadata.capability |
| Reporter | JornDuwel |
| Assignee | Richard Ettema |
| In-Person | — |
| Created | 2025-07-10 |
| Updated | 2026-01-27 |
| Resolved | 2026-01-27 |

---

## ⚠️ Halt: Missing Per-Repo Briefing

This implementation plan **cannot be produced** without fabricating repo facts,
because the authoritative briefing for the in-scope repository is not present.

### Why this halts

- **Resolution:** `Persuasive` — "Will change the constraint to enforce that
  one of the properties must be **true**, rather than just that one must exist."
- The ticket targets `TestScript.metadata.capability` and the invariant on it
  (referred to as `tst-4` in R5; `tst-1` on the element in the current trunk).
- Commenter `corey_spears` (2026-01-17) noted:
  > "This ticket will likely need to be moved to the future Testing IG since
  > the resource has been moved outside of R6 normative."
- Confirmed against the HL7/fhir clone (HEAD `db79dcd`):
  - `source/fhir.ini` no longer lists `TestScript`.
  - `source/ballot-intro.html:94` links `TestScript` to
    `https://build.fhir.org/ig/HL7/fhir-testing-ig/en/StructureDefinition-TestScript.html`.
  - `source/conformance-module.html:125` states TestScript is part of the
    Implementer Support section (now externalised).
  - No `source/testscript/` directory exists.
- Confirmed against the HL7/fhir-testing-ig clone that the canonical
  TestScript StructureDefinition with the exact invariant is now located at
  `input/resources/testscript/StructureDefinition-TestScript.xml`
  (see `element id="TestScript.metadata"` lines 541–557, which carries
  the constraint
  `capability.required.exists() or capability.validated.exists()`).
- Therefore the in-scope repo for implementing this resolution is
  **`HL7/fhir-testing-ig`**, not `HL7/fhir`.

### Briefing status

| Repo | `repo-analysis/briefing.md` | `repo-analysis/meta.json` | Clone present |
|------|------------------------------|----------------------------|---------------|
| `HL7/fhir` | ✅ present, fresh (clone HEAD `db79dcd` + playbook `9c2e8e5` match `meta.json`) | ✅ | ✅ |
| `HL7/fhir-testing-ig` | ❌ **missing** | ❌ **missing** | ✅ (`cache/github/repos/HL7_fhir-testing-ig/clone/`) |

Per the `ticket-plan` skill Step 3:

> If a briefing is **missing** or **stale** for any required repo, **stop
> and ask the user** to run the `repo-analysis` skill before resuming. Do
> not proceed with partial repo context, and do not fabricate repo facts
> to fill the gap.

### Action required

Please run the `repo-analysis` skill against `HL7/fhir-testing-ig` so
that `cache/github/repos/HL7_fhir-testing-ig/repo-analysis/briefing.md`
and `meta.json` are produced, then re-dispatch this ticket:

```
repo-analysis HL7/fhir-testing-ig
```

Once the briefing exists, the plan can be completed with
Authoring root(s), Artifact Map, Recommended Change Recipes, and
Warnings/Gotchas sourced from that briefing.

### Provisional notes (for reference only — not an implementation plan)

These are captured from the Jira ticket and cross-reference data already
gathered; they are **not** a substitute for the missing briefing and must
be re-grounded in the HL7/fhir-testing-ig briefing before any tasks are
written.

- Resolution summary: tighten the invariant from "one of the two flags
  must exist" to "one of the two flags must be **true**", given that
  `TestScript.metadata.capability.required` and
  `TestScript.metadata.capability.validated` are both `boolean 1..1` and
  therefore always exist.
- Likely single-file change in `HL7/fhir-testing-ig`:
  `input/resources/testscript/StructureDefinition-TestScript.xml`,
  `element id="TestScript.metadata"` (lines 541–557 at clone HEAD),
  updating the `<expression>` of the `tst-1` constraint from
  `capability.required.exists() or capability.validated.exists()` to the
  resolution-approved form (expected:
  `capability.all(required = true or validated = true)` or
  `capability.required = true or capability.validated = true` — exact
  FHIRPath to be confirmed during implementation).
- Also update the `<human>` text on that constraint to match the new
  semantics ("contain required or validated or both with value true").
- Cross-references found via `cross-referenced FHIR-51476`: two FHIR
  artifact mentions only (`TestScript.metadata.capability`,
  `TestScript.metadata.capability.required`). No GitHub PRs, issues, or
  Zulip threads are linked to this ticket.

---

**Status:** Halted pending `repo-analysis HL7/fhir-testing-ig`.
