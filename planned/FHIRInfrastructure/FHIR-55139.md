# Implementation Plan: FHIR-55139 — HALTED (missing repo briefing)

| | |
|-|-|
| Ticket | [FHIR-55139](https://jira.hl7.org/browse/FHIR-55139) : Change Request |
| Title | Potentially re-useable THO-candidate value sets in resource ActorDefinition |
| Work Group | FHIR Infrastructure |
| Status | Highest — Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter | Lloyd McKenzie |
| Assignee | Unassigned |
| Created | 2026-01-20 |
| Updated | 2026-01-27 |
| Resolved | 2026-01-27 |

---

## Status: Halted pending `repo-analysis`

Per the `ticket-plan` skill, Step 3 requires a current per-repo briefing for every
in-scope repository. One of the required briefings is **missing** and this plan
cannot proceed without fabricating repo facts.

### Resolution (verbatim, for context)

1. We will add a second top-level code of `system` — "software and/or hardware involved in data exchange".
2. We will provide definitions for the codes that are missing them (referring to the corresponding resource definitions).
3. We will move the code system to THO and will keep the value set in FHIR (it's not re-useable), locked to the specific THO release.

### In-scope repositories (inferred from the resolution)

| Repo | Category | Role | Briefing status |
|-|-|-|-|
| `HL7/fhir` | FhirCore | Owns `ActorDefinition` resource, the `actordefinition-actor-code` ValueSet, and the `ActorDefinition.category` binding. The ValueSet stays in FHIR, locked to a specific THO release. | **OK** — `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` present; `meta.json` clone HEAD `db79dcd…` matches clone, playbook SHA `9c2e8e5…` matches `fhir-core.md`. |
| `HL7/UTG` | UTG | Destination for the migrated `ActorDefinitionActorCode` CodeSystem (THO) and the place where the new `system` code + missing code definitions must be authored. | **MISSING** — no `cache/github/repos/HL7_UTG/repo-analysis/briefing.md` or `meta.json`. |

### Action required

Please run the `repo-analysis` skill for `HL7/UTG` so the briefing is persisted
under `cache/github/repos/HL7_UTG/repo-analysis/`, then re-run `ticket-plan`
for FHIR-55139. Example:

> Run `repo-analysis HL7/UTG` (or `repo-analysis HL7/UTG if-stale`) and let me
> know when the briefing is ready.

No implementation plan has been produced because the skill forbids proceeding
with partial repo context or inventing paths/recipes for a repo whose layout
has not been analysed.

### Data gathered so far (kept for the resumed run)

- Ticket snapshot: `temp/plan/FHIR-55139.json`
- Cross-references: `temp/plan/xref.json` (0 hits — no linked PRs/issues/Zulip threads)
- Keywords: `temp/plan/kw.json` (top terms: `actordefinition`, `actordefinition.category`, `actordefinitionactorcode`, `migration`, `category`, `code`, `system`, `hardware`, `software`)
- Configured-repos call returned `INVALID_ARGUMENT` for `github.repos`; repo inventory taken from `cache/github/repos/` directory listing instead.
