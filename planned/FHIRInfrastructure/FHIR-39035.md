# Implementation Plan: FHIR-39035

**Status:** Halted — missing repo briefing

## Ticket Summary

- **Title:** Define the bind-with-token Message Used in WebSockets Subscription Channel Type
- **Work Group:** FHIR Infrastructure
- **Specification:** FHIR Core (FHIR)
- **Resolution:** Persuasive with Modification
- **Resolution Description:** "We will add detailed examples with literals in the appropriate protocols for each of the channel types, including EBNF notation for the expressions."
- **Labels:** MovingToGuide
- **Latest assignee comment (2025-11-04, Gino Canessa):** "For R6, websocket details are moving out of core into the subscription backport IG. Will mark this ticket applied once the content has been updated and reviewed there."

## Why This Is Halted

Per the assignee's 2025-11-04 comment and the `MovingToGuide` label, the primary in-scope repository for this ticket is **`HL7/fhir-subscription-backport-ig`** (where the WebSocket channel definition, including the bind-with-token message and its EBNF notation, is being authored for R6). A secondary touch point is **`HL7/fhir`** (FHIR Core), where the corresponding WebSocket content is being removed or reduced to a pointer into the IG.

The `ticket-plan` skill (Step 3) requires a current per-repo briefing under
`cache/github/repos/<owner>_<name>/repo-analysis/` for every in-scope
repository before the plan may proceed. The current state is:

| Repo | Briefing status |
|------|-----------------|
| `HL7/fhir` | ✅ Present (`cache/github/repos/HL7_fhir/repo-analysis/briefing.md`, clone HEAD `db79dcdf`, analyzed 2026-04-21) |
| `HL7/fhir-subscription-backport-ig` | ❌ **Missing** — no `repo-analysis/` directory under `cache/github/repos/HL7_fhir-subscription-backport-ig/` |

Per the skill rules, the plan must not be written with partial repo context and repo facts must not be fabricated. The authoring root, artifact map, and recommended change recipes for the subscription backport IG are required to ground concrete task-level file paths (e.g., where the WebSocket channel narrative page and its EBNF block should live in that IG).

## Cross-References Checked

- **Cross-referenced (`FHIR-39035`)**: 0 hits — no GitHub PRs, commits, Jira tickets, or Zulip threads linked.
- **Keywords (top):** `websockets`, `channel`, `ebnf`, `bind`, `movingtoguide`, `subscription` (fhir_path), `token`, `message`, `literal`, `notation`, `schema`, `protocol`, `expression`.

## Requested Action

Please run the `repo-analysis` skill for `HL7/fhir-subscription-backport-ig` (e.g., `repo-analysis HL7/fhir-subscription-backport-ig if-stale`) so that `cache/github/repos/HL7_fhir-subscription-backport-ig/repo-analysis/briefing.md` and `meta.json` exist and are current. Once ready, re-run `ticket-plan FHIR-39035` and the full implementation plan will be produced (covering: where to add the bind-with-token message definition with EBNF notation in the backport IG, example literals per channel type, and the corresponding clean-up in `HL7/fhir`).

## Data Captured For The Resumed Run

Transient artifacts already gathered under `temp/plan/`:

- `FHIR-39035-get.json` — full ticket with comments and snapshot
- `FHIR-39035-xref.json` — cross-reference query (empty result)
- `FHIR-39035-kw.json` — keyword extraction
