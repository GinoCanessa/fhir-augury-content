# Implementation Plan: FHIR-47272

**Resolution:** Persuasive

## Halted — Missing Per-Repo Briefing

This plan could not be completed because the `repo-analysis` briefing for an in-scope
repository is missing. Per the `ticket-plan` skill (Step 3), the plan must not proceed
with partial repo context or fabricate repo facts.

### Ticket Snapshot

| | |
|-|-|
| Ticket | [FHIR-47272](https://jira.hl7.org/browse/FHIR-47272) — Change Request |
| Title | Async-bundle - Accept header meaning |
| Work Group | FHIR Infrastructure |
| Status | Highest / Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive |
| Reporter | Bas van den Heuvel |
| Assignee | Rosemary Hofstede |
| Created | 2024-09-10 |
| Updated | 2025-01-30 |
| Resolved | 2024-10-07 |

### Required Resolution (verbatim from Jira)

1. Replace *"Specifies the format of the optional OperationOutcome Resource response to
   the kick-off request"* with *"Specifies the format of the optional OperationOutcome
   Resource response to the kick-off request as well as the format of the eventual
   asynchronous response."*
2. Add a new sentence under the last paragraph on the status request (before the
   subheadings): *"The status request SHOULD NOT send an Accept header. If it does
   provide one, it SHALL provide the same Accept header as was in the kick-off
   request."*

### Target Repository Identified

The `async-bundle` page referenced by the resolution is not maintained in `HL7/fhir`.
The FhirCore page `source/async.html` only links outward to the API Incubator IG:

```
https://build.fhir.org/ig/HL7/api-incubator/async-bundle.html
```

The authored source of that page lives in the cached clone at:

- `cache/github/repos/HL7_api-incubator-ig/clone/input/pagecontent/async-bundle.html`

The exact strings quoted in the resolution are present there (lines 63–64 for the
`Accept` header bullet; the "Status Request" section begins near line 88 with its
subheadings at `Response - Success`, `Response - Error`, etc.), so this is the
correct in-scope repo.

### Why This Plan Is Halted

- Required briefing: `cache/github/repos/HL7_api-incubator-ig/repo-analysis/briefing.md`
- Required metadata: `cache/github/repos/HL7_api-incubator-ig/repo-analysis/meta.json`
- **Status:** both files are missing (no `repo-analysis/` directory exists for
  `HL7/api-incubator-ig`).

The `HL7/fhir` briefing exists and is fresh (clone HEAD `db79dcd`, playbook SHA
`9c2e8e5`, both match), but its Ticket-Relevant Paths were scoped to FHIR-42980
(`source/mapping-language.html`) and it explicitly states this ticket’s page is hosted
in the API Incubator — not in `HL7/fhir`. The FhirCore briefing therefore cannot ground
the implementation plan for FHIR-47272.

### Action Required Before Resuming

Please run the `repo-analysis` skill for the API Incubator IG, e.g.:

> Run `repo-analysis HL7/api-incubator-ig` (or `if-stale`) and let me know when the
> briefing has been written to
> `cache/github/repos/HL7_api-incubator-ig/repo-analysis/briefing.md`.

Once that briefing is available, re-run `ticket-plan FHIR-47272` and it will produce
the full implementation plan grounded in the API Incubator IG’s authoring root,
recipes, and gotchas.

### Data Gathered So Far (for continuity)

- Jira snapshot: `temp/plan/FHIR-47272.json`
- Cross-references: `temp/plan/xrefs.json` — 0 hits (no GitHub PRs, Jira links, or
  Zulip threads reference this ticket in the cached indexes).
- Keywords: `temp/plan/keywords.json` — dominated by `header`, `accept`, `format`,
  `async`, `kick`, `operationoutcome`, `bundle`, `status`, confirming the scope is
  the async-bundle Accept/Content-Type documentation.
