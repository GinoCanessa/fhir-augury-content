# Implementation Plan: FHIR-47645 — HALTED (missing repo briefing)

| | |
|-|-|
| Ticket | [FHIR-47645](https://jira.hl7.org/browse/FHIR-47645) : Change Request |
| Title | Cross version extensions - split off this section in separate IG |
| Work Group | FHIR Infrastructure |
| Status | Highest — Resolved - change required |
| Resolution | Persuasive with Modification |
| Specification | FHIR Core (FHIR) |
| Labels | MovingToGuide |
| Assignee | Gino Canessa |
| Reporter | Bas van den Heuvel |
| Created | 2024-09-13 |
| Resolved | 2024-09-24 |
| Updated | 2025-11-04 |

## Halt Reason

Per the `ticket-plan` skill (Step 3) and the orchestrator's critical rules, the
plan **cannot be produced** until a current per-repo briefing exists for every
in-scope repository. The plan must be grounded in `repo-analysis/briefing.md`
files; fabricating repo facts is not allowed.

### Resolution (verbatim)

> We will retain the existing cross-version extensions section in the core spec
> to talk about the fact that it is possible and to explain the basics of how
> it works. We will point from there to a new page in the extensions pack
> where we'll provide detailed rules for how the conversion packages are
> created, including taking into account things like name changes.

This resolution explicitly touches **two** repositories:

1. **`HL7/fhir`** — trim the existing cross-version extensions section in the
   core spec down to a brief intro that describes the mechanism and links out.
2. **`HL7/fhir-extensions`** ("the extensions pack") — add a new page
   containing the detailed authoring rules for cross-version conversion
   packages (including handling of element/resource renames).

### Briefing status

| Repo | Briefing path | Status |
|------|---------------|--------|
| `HL7/fhir` | `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` | ✅ Present and current (clone HEAD `db79dcdf` matches `meta.json`; playbook SHA `9c2e8e5b` matches `.github/skills/repo-analysis/categories/fhir-core.md`) |
| `HL7/fhir-extensions` | `cache/github/repos/HL7_fhir-extensions/repo-analysis/briefing.md` | ❌ **Missing** — only `clone/` exists under `cache/github/repos/HL7_fhir-extensions/`; no `repo-analysis/` directory has been generated |

### Action required before this plan can be produced

Run the `repo-analysis` skill for the missing repo, e.g.:

```text
repo-analysis HL7/fhir-extensions
```

Once `cache/github/repos/HL7_fhir-extensions/repo-analysis/briefing.md` and
`meta.json` exist (and `meta.json` matches the current clone HEAD + playbook
SHA), re-dispatch `ticket-plan FHIR-47645` and the full plan will be produced.

## Data gathered so far (for the eventual plan)

- **Cross-references:** `cross-referenced FHIR-47645` returned 0 hits — no
  GitHub PRs/issues, related Jira tickets, or Zulip threads have been linked
  to this ticket in the augury index. The plan will need to rely solely on
  the resolution text and the per-repo briefings.
- **Keywords:** dominated by `cross`, `version`, `extension`, `pack`,
  `section`, `split`, `companion`, `conversion`, `MovingToGuide` —
  consistent with a documentation/IG-restructure ticket rather than a
  resource/element schema change.
- **Comments of note:**
  - 2024-09-24 (ginocanessa): "Discussed at FHIR-I WGM Tuesday Q4. Decision
    was to hold off on this ticket until we have the cross-version extensions
    published and then decide (before R6 is published)."
  - 2025-11-04 (ginocanessa): "Will update this ticket when content is
    confirmed in the target guides."

No further work was performed. Re-run after the missing briefing has been
generated.
