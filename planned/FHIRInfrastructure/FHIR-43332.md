# Implementation Plan: FHIR-43332

**Status:** Halted — required repo briefing missing.

## Halt Reason

The Jira resolution for [FHIR-43332](https://jira.hl7.org/browse/FHIR-43332)
("Clients should have a way to detect supported sort order", resolution
**Persuasive with Modification**) explicitly homes the change in the
**CapabilityStatement 2 / Feature Query IG**:

> Discussed in multiple quarters at the WGM. Agree that this functionality
> should exist and that the best home is in the CapabilityStatement 2 /
> Feature Query IG. Will ensure that a 'core' feature is defined for this.

The repository that hosts the CapabilityStatement 2 / Feature Query IG is
**`HL7/capstmt`**. The clone exists at
`cache/github/repos/HL7_capstmt/clone/`, but the per-repo briefing
required by Step 3 of the `ticket-plan` skill is **missing**:

- `cache/github/repos/HL7_capstmt/repo-analysis/briefing.md` — **not present**
- `cache/github/repos/HL7_capstmt/repo-analysis/meta.json` — **not present**

Per the skill's "Important Rules", planning must not proceed with partial
repo context or fabricated repo facts.

The `HL7/fhir` briefing was checked and is fresh
(`clone_head_sha = db79dcdfe196d35bd0e74c58c655a4c1c8f534f7`,
`playbook_sha = 9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4`), so no halt is
required for FHIR Core.

## Action Required

Please run the `repo-analysis` skill for the missing repo and re-dispatch
this ticket:

> `repo-analysis HL7/capstmt`

## Context Gathered (for resumption)

- **Ticket:** FHIR-43332 — *Clients should have a way to detect supported sort order*
- **Type / Priority:** Change Request / Medium
- **Work Group:** FHIR Infrastructure
- **Specification:** FHIR Core (FHIR)
- **Labels:** `CapabilityStatement2`
- **Resolution:** Persuasive with Modification (resolved 2024-05-23)
- **Reporter:** Elliot Silver
- **Assignee:** Gino Canessa
- **Cross-references (Step 1b):** 1 Zulip thread
  (`[implementers] Apply sort on Extension`, id `405872676`); no GitHub
  PRs/issues/commits reference this ticket.
- **Top keywords (Step 1c):** `sort`, `search`, `parameters` (fhir_path),
  `capabilitystatement2`, `feature`, `capabilitystatement` (fhir_path),
  `ascend`, `descend`, `client`, `support`.
- **In-scope repo (Step 2):** `HL7/capstmt` (CapabilityStatement 2 /
  Feature Query IG) — derived from the resolution text and the
  `CapabilityStatement2` label. `HL7/fhir` is potentially in scope only
  if the resolution's clarifying text about "the relationship between
  search parameters and sort" is taken as a documentation change to the
  base spec; the resolution itself only commits to defining a core
  feature in the Feature Query IG.

Once `HL7/capstmt` has a current briefing, this skill can be re-run to
produce the full plan.
