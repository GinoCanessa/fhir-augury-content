# Implementation Plan: FHIR-46130

**Status:** Halted — required per-repo briefing missing.

## Required action

Run the `repo-analysis` skill for the following repository, then re-run this
plan:

- **`HL7/UTG`** — no briefing found at
  `cache/github/repos/HL7_UTG/repo-analysis/briefing.md`.

## Why this repo is required

FHIR-46130 changes the `description` text of the `not-applicable` concept in
the `data-absent-reason` CodeSystem. That CodeSystem is hosted in the
HL7 Unified Terminology Governance repository (`HL7/UTG`), not in
`HL7/fhir` — the `HL7/fhir` clone only carries the matching ValueSet
(`source/terminologies/valueset-data-absent-reason.xml`). The OO applied-vote
comment (jdlnolen, 2025-09-09) explicitly states "We agree and recommend
these changes, and recommend that FHIR-I submit a UTG ticket for this
change", confirming that the CodeSystem edit lands in UTG.

Per the `ticket-plan` skill (Step 3), this skill is data-only with respect
to `repo-analysis` and must stop rather than fabricate UTG repo facts or
file paths.

## Briefing status snapshot

| Repo | Briefing | Notes |
|------|----------|-------|
| `HL7/fhir` | Present and fresh (`clone_head_sha` `db79dcdfe1…`, `playbook_sha` `9c2e8e5b…`) | Holds only `source/terminologies/valueset-data-absent-reason.xml`; no CodeSystem authoring source for `data-absent-reason`. |
| `HL7/UTG` | **Missing** (`cache/github/repos/HL7_UTG/repo-analysis/` does not exist) | Required — owns `CodeSystem-data-absent-reason`. |

## Ticket snapshot (for context once briefing is regenerated)

| | |
|-|-|
| Ticket | [FHIR-46130](https://jira.hl7.org/browse/FHIR-46130) — Change Request |
| Title | Remove (e.g., …) from Data Absent Reason `not-applicable` description |
| Work Group | FHIR Infrastructure |
| Specification | FHIR Core (FHIR) |
| Status | Medium — Resolved - change required |
| Resolution | Persuasive with Modification |
| Reporter | Carmela A. Couderc |
| Assignee | Unassigned |
| Created | 2024-06-11 |
| Updated | 2025-09-18 |

**Resolution intent (from comments):** replace the description of code
`not-applicable` in CodeSystem `data-absent-reason` from
*"There is no proper value for this element (e.g. last menstrual period for
a male)."* to *"There is no proper value for this element"*, and add a
`comment` on the concept reading *"For example, an occupation for an
infant."* Cross-references: none surfaced. OO also asks that FHIR-I take
ownership of the `data-absent-reason` CodeSystem (currently `oo`), which
would be expressed via the `structuredefinition-wg` extension on the
CodeSystem in UTG.

Once `repo-analysis HL7/UTG` has been run and its briefing exists at
`cache/github/repos/HL7_UTG/repo-analysis/briefing.md`, re-run this
ticket-plan to produce the full implementation plan.
