# Implementation Plan: FHIR-47277 — HALTED (missing repo briefing)

| | |
|-|-|
| Ticket | [FHIR-47277](https://jira.hl7.org/browse/FHIR-47277) : Change Request |
| Title | Async-bundle -> mandate single outcome behavior |
| Work Group | FHIR Infrastructure |
| Status | Highest · Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive |
| Reporter | Bas van den Heuvel |
| Assignee | Bas van den Heuvel |
| Created | 2024-09-10 |
| Updated | 2024-11-04 |
| Resolved | 2024-10-22 |

## Halt Reason

Per the `ticket-plan` skill, Step 3 requires a current per-repo briefing
(`cache/github/repos/<owner>_<name>/repo-analysis/briefing.md` +
`meta.json`) for every in-scope repository. The primary in-scope
repository for this ticket is **`HL7/api-incubator-ig`** (category
`Incubator`, per
`src/FhirAugury.Source.GitHub/appsettings.json` →
`IncubatorRepositories`), but that briefing is **missing**:

- `cache/github/repos/HL7_api-incubator-ig/clone/` — present (clone exists).
- `cache/github/repos/HL7_api-incubator-ig/repo-analysis/briefing.md` — **missing**.
- `cache/github/repos/HL7_api-incubator-ig/repo-analysis/meta.json` — **missing**.

Per the skill's Step 3 rule ("Do not proceed with partial repo context,
and do not fabricate repo facts to fill the gap"), planning is halted.

> Please run `repo-analysis HL7/api-incubator-ig` (category `Incubator`)
> and re-invoke `ticket-plan FHIR-47277` once the briefing is in place.

The `HL7/fhir` briefing **is** fresh (clone HEAD
`db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` and playbook SHA
`9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4` both match
`cache/github/repos/HL7_fhir/repo-analysis/meta.json`), but it is
ticket-scoped to FHIR-42980 and does not by itself cover the file the
FHIR-47277 resolution actually edits.

## Data Gathered So Far

Collected via `fhir-augury-cli` against the orchestrator at
`http://localhost:5150`.

### Resolution (verbatim)

> Upgrade SHOULDs to SHALLs, as proposed.
>
> `> A successful completion of the status request SHALL be used
> whenever processing has completed, regardless of whether the
> underlying interaction has succeeded or failed. A server SHALL use
> the status and outcome elements of Bundle.entry[0].response to
> communicate any processing errors.`

### Cross-references

One hit from `cross-referenced FHIR-47277` (limit 50):

- `jira FHIR-47277` → `fhir Bundle.entry` (linkType `mentions`,
  context: "...A server SHOULD use the status and outcome elements of
  Bundle.entry [0] .response to communicate any processing errors...").

No GitHub PRs/issues/commits, no Zulip threads, no related Jira tickets
are linked to FHIR-47277 in the cross-reference index.

### Top keywords

`bundle.entry` (fhir_path, 3), `bundle` (fhir_path, 8), `outcome`,
`status`, `async`, `processing`, `shoulds`, `shalls`, `response`,
`server`. Consistent with an edit to the async-bundle status-request
prose.

### In-scope repository inference

- **`HL7/api-incubator-ig`** (Incubator) — confirmed to host the target
  file. `input/pagecontent/async-bundle.html` (in the cached clone at
  `cache/github/repos/HL7_api-incubator-ig/clone/`) contains the exact
  SHOULD/SHALL sentence from the ticket around lines 179–181 under the
  `Response - Complete Status` / "outcome of the kick-off request"
  subsection, with the resolution text wording (`SHALL`) appearing to
  already be present in the current `master`. The plan needs the
  briefing to confirm authoring roots, publisher conventions, and any
  cross-repo touch points before specifying tasks.
- **`HL7/fhir`** (FhirCore) — `source/async.html` (in
  `cache/github/repos/HL7_fhir/clone/`) is the FHIR Core page that
  links out to the async-bundle pattern hosted in api-incubator-ig. It
  does not itself contain the SHOULD/SHALL sentence, but any wording
  change in api-incubator-ig should be checked against
  `source/async.html` for stale language. `HL7/fhir`'s briefing is
  fresh and available at
  `cache/github/repos/HL7_fhir/repo-analysis/briefing.md`.

## Suggested Next Action

Run:

```bash
# Refresh / create the missing briefing for the primary repo
repo-analysis HL7/api-incubator-ig
```

Then re-run `ticket-plan FHIR-47277`. At that point the full plan can
be produced, grounded in the `HL7/api-incubator-ig` briefing's
Authoring root, Recommended Change Recipes, and Warnings/Gotchas,
alongside the already-available `HL7/fhir` briefing for any
cross-linked prose in `source/async.html`.
