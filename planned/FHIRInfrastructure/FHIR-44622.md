# Implementation Plan: FHIR-44622

| | |
|-|-|
| Ticket | [FHIR-44622](https://jira.hl7.org/browse/FHIR-44622) : Change Request |
| Work Group | FHIR Infrastructure |
| Status | Medium High — Resolved - change required |
| Labels | CapabilityStatement2, R5 |
| Specification | FHIR Core (FHIR) |
| Related Artifacts | *(none reported by Augury)* |
| Related Pages | *(none reported by Augury)* |
| Related URLs | *(none reported by Augury)* |
| Related Sections | *(none reported by Augury)* |
| Reporter | Robert Snelick |
| Assignee | Unassigned |
| In-Person | *(none)* |
| Created | 2024-01-30 |
| Updated | 2025-11-06 |
| Resolved | 2024-04-01 |

---

## Halt Note — Repo Briefing Missing

**Planning was halted at Step 3 of the `ticket-plan` skill.** The skill's
critical rule is: *if the per-repo briefing is missing or stale for any
in-scope repository, STOP and write a short halt-note — do not fabricate
repo facts.* That condition is met here.

### Resolution Summary (from Jira)

> **Resolution:** Persuasive with Modification
>
> *"We will add a tracker item to the CapabilityStatement2 IG to include
> this ability to identify different profiles for different RESTful
> interactions for a given resource."*

The resolution explicitly redirects the implementation work from FHIR
Core (the ticket's reported specification) into the **CapabilityStatement2
IG**. No change is expected in `HL7/fhir`; the substantive design work
is deferred to a new tracker item (follow-up Jira issue) filed against
the CapabilityStatement2 IG project, where the concrete extension /
resource shape will be worked out.

### In-Scope Repository (inferred from the resolution)

| Repo | Expected role | Rationale |
|------|---------------|-----------|
| `HL7/capstmt` | Home of the CapabilityStatement2 IG (now published as the *FHIR Application Feature Framework* IG, canonical `http://hl7.org/fhir/uv/application-feature`). | The resolution names "CapabilityStatement2 IG" as the destination for the follow-up work; `HL7/capstmt` is the cached clone corresponding to that IG. |

FHIR Core (`HL7/fhir`) is **not** in scope for this ticket per the
resolution: the FHIR Infrastructure WG chose to defer the profile-per-
interaction capability to the IG rather than change the core
`CapabilityStatement` resource.

### Cross-References and Related Work (from Augury)

- `cross-referenced FHIR-44622` → **0 hits.** No GitHub PRs, commits,
  Jira tickets, Confluence pages, or Zulip threads are linked to this
  ticket in the current Augury index. A companion tracker item against
  the CapabilityStatement2 IG may exist in Jira but is not linked here.
- Top keywords (BM25): `interaction`, `obligation`, `capability`,
  `statement`, `profile`, `capabilitystatement2`, `Patient`
  (`fhir_path`) — consistent with the resolution scope.

### Why the skill halted

Step 3 requires, for every in-scope repo, that the persisted briefing
and metadata exist under
`cache/github/repos/<owner>_<name>/repo-analysis/`.

Check performed:

- `cache\github\repos\HL7_capstmt\repo-analysis\briefing.md` — **missing**
- `cache\github\repos\HL7_capstmt\repo-analysis\meta.json` — **missing**

(The `HL7_capstmt` clone itself is present at
`cache\github\repos\HL7_capstmt\clone\` — `sushi-config.yaml` identifies
it as IG `hl7.fhir.uv.application-feature`, FHIR R5, STU1 — but no
`repo-analysis` artifacts have been produced for it.)

Without a current briefing, the skill cannot ground the Feature
Proposal, Repo Context, Impact Analysis, or Implementation Plan in
verified authoring roots, artifact map, recipes, or gotchas.

### Requested Action

Please run the `repo-analysis` skill against the in-scope repo before
resuming planning:

```text
repo-analysis HL7/capstmt if-stale
```

Once the briefing + `meta.json` are produced under
`cache/github/repos/HL7_capstmt/repo-analysis/`, re-run
`ticket-plan FHIR-44622` and the full feature proposal / impact
analysis / step-by-step plan will be produced against the refreshed
repo context.

### Provisional Shape of the Plan (for context only — not grounded)

To help scope the re-run, the *shape* of the eventual plan — **not yet
validated against a briefing** and therefore not to be acted on — is
expected to be:

1. A new Jira tracker item filed against the CapabilityStatement2 IG /
   Application Feature Framework IG project, referencing this ticket.
2. Design work in `HL7/capstmt` to allow per-profile RESTful
   interaction codes, most likely via a FHIR extension applied to
   `CapabilityStatement.rest.resource.supportedProfile` (or an
   equivalent IG-defined structure), so each listed profile can carry
   its own set of `interaction.code` values rather than inheriting the
   resource-level list.
3. IG narrative updates explaining the new extension, usage examples
   mirroring "Table 2" in the ticket description, and at least one
   worked example `CapabilityStatement` instance.

All specifics (exact file paths, extension URL, StructureDefinition
location, FSH vs. XML authoring, template/tooling gotchas) must come
from the `HL7/capstmt` briefing and are intentionally not filled in
here.
