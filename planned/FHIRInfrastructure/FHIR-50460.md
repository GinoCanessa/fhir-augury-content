# Implementation Plan: FHIR-50460

| | |
|-|-|
| Ticket | [FHIR-50460](https://jira.hl7.org/browse/FHIR-50460) : Technical Correction |
| Title | "NE" flag mentioned but not defined |
| Work Group | FHIR Infrastructure |
| Status | Medium · Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive |
| Reporter | James Jahns |
| Assignee | Unassigned |
| Created | 2025-04-29 |
| Updated | 2026-04-13 |
| Resolved | 2026-04-13 |

## ⚠️ Halt — Missing Repo Briefing

This plan was **halted at Step 3** (Load Saved Per-Repo Briefings) because a
required briefing is missing.

### Why

The ticket's only GitHub cross-reference (and the entire content of the
resolution description) points at **`HL7/fhir-ig-publisher#1293`**:

> Resolution: *Persuasive*
> Resolution description: "Raised <https://github.com/HL7/fhir-ig-publisher/issues/1293>"
>
> Cross-reference (`cross-referenced FHIR-50460`):
> `jira:FHIR-50460 → github:HL7/fhir-ig-publisher#1293` (`mentions`, score 0.9997)

So `HL7/fhir-ig-publisher` is in scope per Step 2. However:

- `cache/github/repos/HL7_fhir-ig-publisher/repo-analysis/briefing.md` — **does not exist**
- `cache/github/repos/HL7_fhir-ig-publisher/repo-analysis/meta.json` — **does not exist**
- The repo is not cloned under `cache/github/repos/` at all (verified by
  listing that directory; only the 60 currently-cached repos are present
  and `HL7_fhir-ig-publisher` is not among them).

Per the `ticket-plan` skill, Step 3:

> If a briefing is **missing** or **stale** for any required repo, **stop
> and ask the user** to run the `repo-analysis` skill before resuming …
> Do not proceed with partial repo context, and do not fabricate repo
> facts to fill the gap.

### What is needed to resume

1. Add `HL7/fhir-ig-publisher` to the FhirAugury GitHub cache (clone +
   ingestion) so it appears under `cache/github/repos/HL7_fhir-ig-publisher/clone/`.
2. Run the `repo-analysis` skill against `HL7/fhir-ig-publisher` to
   produce `repo-analysis/briefing.md` + `meta.json`.
3. Re-dispatch this ticket to `ticket-plan`.

### Verified facts (kept for the resumed run)

These are the data points already gathered via `fhir-augury-cli` and
direct reads of the `HL7/fhir` clone — they do **not** require the
missing briefing and can be reused without re-fetching:

- **Original problem (from ticket description).** The FHIR spec page
  describing the column/flag conventions for resource definitions
  contains the sentence:
  > "Extensions are not always shown, but may appear except where the
  > flag `NE` appears"
  but the `NE` flag is **not listed in the "Key to Flags"** legend
  immediately above that paragraph, and (per the reporter) is not in
  use anywhere. The reporter proposed two options: (a) remove the
  reference to `NE`, or (b) add `NE` to the "Key to Flags".
- **Where the offending text actually lives in `HL7/fhir`.** Confirmed
  by grep of the cached clone (clone HEAD `db79dcd`):
  - `source/formats.html` line **163** — the `<li>` containing
    "…except where the flag `<code>NE</code>` appears".
  - `source/formats.html` lines **139–151** — the `<p>Key to Flags</p>`
    list. Currently documents `?!`, `S`, `Σ`, `I`, `T`, `«A»`, `«I»`,
    `TU`, `N`, `D`. There is **no `NE` entry**.
- **HL7/fhir briefing freshness.** `cache/github/repos/HL7_fhir/repo-analysis/meta.json`
  reports `clone_head_sha=db79dcdfe196d35bd0e74c58c655a4c1c8f534f7`
  and `playbook_sha=9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4`; both
  match the current clone HEAD and the current
  `.github/skills/repo-analysis/categories/fhir-core.md` blob hash, so
  the FhirCore briefing is fresh. The briefing is, however, scoped to
  ticket `FHIR-42980`, so its `Ticket-Relevant Paths` section does not
  cover this ticket — only the generic Facts (authoring root `source/`,
  bespoke publisher, `source/*.html` pages are authored content) are
  reusable.
- **Ticket cross-references.** Only one: `HL7/fhir-ig-publisher#1293`
  (`mentions`). No related Zulip threads, no related Jira tickets, no
  Confluence pages. No comments on the Jira ticket itself.
- **Keywords (top 5 by BM25):** `ne` (word, 7), `flag` (fhir_path, 9),
  `tt` (word, 6), `mention` (word, 4), `attribute` (word, 2).

### Open question for the resumer

Once the `HL7/fhir-ig-publisher` briefing is available, the plan will
need to decide whether the resolution actually requires:

- **Option A — IG-Publisher-only fix.** Define and emit the `NE` flag
  from the IG publisher (the rendering side), so the existing prose in
  `source/formats.html` becomes accurate. This is what the resolution
  description implies (the only deliverable named is the
  `fhir-ig-publisher` issue).
- **Option B — Spec-page-only fix.** Edit
  `source/formats.html` (line 163) in `HL7/fhir` to either drop the
  "…except where the flag `NE` appears" clause or add an `NE` entry to
  the `Key to Flags` list at lines 140–151.
- **Option A + B.** Both repositories change in lockstep.

Without `HL7/fhir-ig-publisher#1293`'s body and any commits/PRs linked
from it, the chosen option cannot be confirmed from cached data. The
resumer should fetch that issue first and let it drive whether the
`HL7/fhir` edits described above are also in scope.
