# Implementation Plan: FHIR-54869 — HALTED

**Ticket:** [FHIR-54869](https://jira.hl7.org/browse/FHIR-54869) — *NutritionIntake not listed as Workflow Event*
**Work Group:** FHIR Infrastructure
**Specification:** FHIR Core (FHIR)
**Resolution:** Persuasive (Resolved - change required)
**Type:** Technical Correction

## Halt Reason

Per the `ticket-plan` skill (Step 3), the per-repo briefing for the in-scope
repository **`HL7/fhir`** is **stale** and the plan cannot proceed without
re-running `repo-analysis`.

### Staleness evidence

`cache/github/repos/HL7_fhir/repo-analysis/meta.json` records:

| Field | Saved value | Current value | Match |
|-------|-------------|---------------|-------|
| `clone_head_sha` | `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` | `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` | ✅ |
| `playbook_sha` (`.github/skills/repo-analysis/categories/fhir-core.md`) | `9c2e8e5b44b00d6ecf3cc04a2904e13c537b0fa4` | `67043498b4b3ef0c4990b7705c85aa0dbe6fbc7a` | ❌ |
| `ticket_key` | `FHIR-42980` | `FHIR-54869` | ❌ (Ticket-Relevant Paths/Recipes are scoped to the prior ticket) |

The `repo-analysis` staleness rule requires both the clone HEAD **and** the
playbook SHA to match. The playbook has changed since the last analysis, so
the cached `briefing.md` may no longer reflect the current category playbook,
and the `Ticket-Relevant Paths` / `Recommended Change Recipes` sections are
tailored to FHIR-42980 (FHIR Mapping Language prose) rather than to this
ticket's scope (workflow page event-resource list).

### Required action

Please run the `repo-analysis` skill against `HL7/fhir`, scoped to this
ticket, before resuming the plan:

```
repo-analysis HL7/fhir if-stale --ticket FHIR-54869
```

Once `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` and `meta.json`
are refreshed (matching the current `fhir-core.md` playbook SHA and
recording `ticket_key: FHIR-54869`), re-invoke `ticket-plan FHIR-54869` and
the plan will be produced with grounded paths and recipes.

## Ticket Snapshot (for context only)

The new `NutritionIntake` resource (FHIR R6 ballot 4) declares that it
implements the **Event** workflow pattern, but it is not listed among the
Event Resources on the `workflow.html` (Workflow) specification page in
`HL7/fhir`. The Persuasive resolution requires adding `NutritionIntake` to
the Event Resources list on that page. No GitHub, Zulip, or related-Jira
cross-references were returned for this ticket
(`cross-referenced` total = 0).

Concrete file paths, recipes, and per-task instructions will be filled in
once the briefing for `HL7/fhir` is refreshed — they are intentionally
omitted here to avoid fabricating repo facts.

---

*Generated by the `ticket-plan` skill. Halted at Step 3 (briefing
staleness check) per skill rules: "Do not proceed with partial repo
context, and do not fabricate repo facts to fill the gap."*
