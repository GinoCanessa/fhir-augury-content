# Implementation Plan: FHIR-45445 — Halted

| | |
|-|-|
| Ticket | [FHIR-45445](https://jira.hl7.org/browse/FHIR-45445) : Change Request |
| Title | Create naming convention rules for FHIR parameters |
| Work Group | FHIR Infrastructure |
| Status | Medium · Resolved - change required |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive |
| Reporter | Sarah Gaunt |
| Assignee | Unassigned |
| Created | 2024-05-02 |
| Updated | 2024-06-24 |
| Resolved | 2024-06-24 |

## Halt: Required Repo Briefing Missing

The ticket-plan skill **stopped at Step 3** because the per-repo briefing required
to ground the implementation plan is not available in the local cache.

### Resolution target

The Jira `resolution_description` explicitly scopes the change to the
**ig-guidance** IG:

> We will update the ig-guidance IG to declare the following:
>
> SearchParameters SHOULD follow these naming rules for the 'code' element…
> (all lower-case; no underscore except as prefix for shared/base parameters;
> separate words with dashes; align with resource element name converting
> camelCase to dash-case; be consistent with similar search parameters in
> other resources).
>
> Operation parameter names should follow these naming rules for
> parameter.name: lowerCamelCase; no underscore or dashes; align with the
> corresponding resource element name when the operation parameter
> corresponds to a single element; be consistent with similar parameter
> names in other operations.
>
> We will also create a ticket for the validator to spit out a best practice
> message when these rules aren't followed.

The authoritative source repo for that IG in the local cache is
`FHIR/ig-guidance` (cached at `cache/github/repos/FHIR_ig-guidance/clone/`).
There are no GitHub cross-references recorded for FHIR-45445
(`cross-referenced` returned 0 hits), so this is the single in-scope repo.

### Missing briefing

- **Expected:** `cache/github/repos/FHIR_ig-guidance/repo-analysis/briefing.md`
  and `cache/github/repos/FHIR_ig-guidance/repo-analysis/meta.json`
- **Actual:** the `FHIR_ig-guidance/repo-analysis/` directory does not exist.
  Only the `clone/` subdirectory is present.

Per the ticket-plan skill's Step 3 rule, this agent must not fabricate
authoring roots, artifact maps, recommended change recipes, or
ticket-relevant paths. Proceeding without the briefing would violate the
"data-only" contract between `ticket-plan` and `repo-analysis`.

### Action required

Please run the `repo-analysis` skill for the in-scope repository before
resuming, then re-invoke `ticket-plan` for FHIR-45445:

```text
repo-analysis FHIR/ig-guidance if-stale
```

Once `cache/github/repos/FHIR_ig-guidance/repo-analysis/briefing.md` and
`meta.json` are present and current against the clone HEAD, re-running this
plan will produce the full Feature Proposal, Repo Context, Impact Analysis,
and Step-by-Step Tasks required by the skill's Report Format.

### Data gathered before halt (for continuity)

- **Ticket fetch:** `temp/plan/FHIR-45445-get.json` (full content, comments,
  snapshot — resolution is `Persuasive`, so the resolution-type guard did
  NOT trigger a stub; this halt is purely due to missing repo context).
- **Cross-references:** `temp/plan/FHIR-45445-xref.json` — 0 hits. No
  GitHub PRs, related Jira tickets, or Zulip threads reference this
  ticket key.
- **Keywords:** `temp/plan/FHIR-45445-kw.json` — top terms include
  `dash`, `name`, `underscore`, `convention`, `rule`, `parameters`
  (fhir_path), `lowerCamelCase`, `camelCase`, `searchparameters`,
  `validator`. These confirm the scope: SearchParameter.code naming and
  OperationDefinition.parameter.name naming guidance.
- **FMG context link** (from the ticket body):
  `https://confluence.hl7.org/pages/viewpage.action?pageId=239208731`.
- **Follow-up ticket commitment:** the resolution also commits to filing a
  separate ticket for the validator to emit a best-practice warning when
  the rules are violated. That validator ticket is a downstream dependency,
  not part of this ig-guidance change.

## Open Questions

- None blocking from the ticket itself; the resolution text is specific
  and self-contained. The only blocker is the missing `repo-analysis`
  briefing for `FHIR/ig-guidance`.
