# Implementation Plan: FHIR-43657

**Status:** Halted — required per-repo briefing missing.

| | |
|-|-|
| Ticket | [FHIR-43657](https://jira.hl7.org/browse/FHIR-43657) : Change Request |
| Title | $graph operation is underdefined |
| Work Group | FHIR Infrastructure |
| Status | Highest — Resolved - change required |
| Labels | AdditionalResources |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Resolution Description | Will change type for 'graph' parameter from 'uri' to 'canonical(GraphDefinition)' |
| Reporter | Bas van den Heuvel |
| Assignee | Corey Spears |
| Created | 2024-01-16 |
| Updated | 2025-10-20 |
| Resolved | 2024-03-04 |

## Halt Reason

The `$graph` OperationDefinition that this resolution edits does **not** live in the `HL7/fhir` core clone in this cache. It is hosted in the `HL7/api-incubator-ig` repository (the ticket carries the `AdditionalResources` label, and `source/ballot-intro.html` line 107 in `HL7/fhir` links `Resource.$graph` to `https://build.fhir.org/HL7/api-incubator/OperationDefinition-Resource-graph.html`). The actual file is present in the cached clone:

- `cache/github/repos/HL7_api-incubator-ig/clone/incoming/operationdefinition-Resource-graph.xml`
  - Line 40–46: the `graph` in-parameter currently declares `<type value="uri"/>` — exactly the element the resolution requires changing.

However, **no per-repo briefing exists for `HL7/api-incubator-ig`**:

- Missing: `cache/github/repos/HL7_api-incubator-ig/repo-analysis/briefing.md`
- Missing: `cache/github/repos/HL7_api-incubator-ig/repo-analysis/meta.json`
- Clone HEAD (for the eventual `meta.json`): `41e1bba2b601d459c6989f9fc89e8d72dafa3517`

Per the `ticket-plan` skill (Step 3): when a briefing is missing or stale for any in-scope repo, the skill must stop and ask the user to run `repo-analysis` rather than fabricate repository facts (authoring root, generated areas, recipes, gotchas).

The only repo with a current briefing in scope of FHIR Core is `HL7/fhir` (clone `db79dcd`, playbook `9c2e8e5`, both matching `meta.json`), and that repo does not contain the `$graph` OperationDefinition. Producing a plan against `HL7/fhir` alone would mis-locate the change.

### Action requested

Run `repo-analysis HL7/api-incubator-ig` (or `repo-analysis HL7/api-incubator-ig if-stale`) to populate the briefing, then re-dispatch this ticket. Once the briefing is in place, the plan can ground the proposed parameter retype (uri → `canonical(GraphDefinition)`) in that repo's authoring root, change recipes, and gotchas.

## Data Gathered (for the resumed run)

- **Ticket (Step 1a):** fetched via `fhir-augury-cli get` — `temp/plan/FHIR-43657-ticket.json`. Resolution `Persuasive with Modification`; resolution description: *"Will change type for 'graph' parameter from 'uri' to 'canonical(GraphDefinition)'."* No comments. Description notes: "Please specify the allowed content of the input parameter (Reference, Canonical, or text). We should consider whether three optional parameters is better than an overloaded one."
- **Cross-references (Step 1b):** `temp/plan/FHIR-43657-xref.json` — `total: 0`. No GitHub PRs/issues, no related Jira tickets, no Zulip threads recorded.
- **Keywords (Step 1c):** `temp/plan/FHIR-43657-kw.json` — top keywords `$graph` (fhir_operation), `graphdefinition` (fhir_path), `graph`, `additionalresources`, `cannonical`, `overload`, `parameter`, `uri`, `canonical`. These confirm the operation/parameter target and the `AdditionalResources` channel.
- **In-scope repos (Step 2):** `HL7/api-incubator-ig` (location of `operationdefinition-Resource-graph.xml`, evidenced by `HL7/fhir/source/ballot-intro.html:107`). `HL7/fhir` is *not* the editing target for the OperationDefinition itself, though it may need an eventual cross-link refresh once the operation is republished.
- **Briefings (Step 3):**
  - `HL7/fhir` — present and current (`cache/github/repos/HL7_fhir/repo-analysis/briefing.md` @ clone `db79dcd`, playbook `9c2e8e5`); does not contain authoring guidance for `incoming/operationdefinition-Resource-graph.xml`.
  - `HL7/api-incubator-ig` — **missing**. Halt.

No further steps were executed.
