# Implementation Plan: FHIR-52745

| | |
|-|-|
| Ticket | [FHIR-52745](https://jira.hl7.org/browse/FHIR-52745) : Change Request |
| Work Group | FHIR Infrastructure |
| Status | Medium Resolved - change required |
| Labels | SDC |
| Specification | FHIR Core (FHIR) |
| Resolution | Persuasive with Modification |
| Reporter | Bas van den Heuvel |
| Assignee | Unassigned |
| Created | 2025-09-08 |
| Updated | 2026-02-05 |
| Resolved | 2026-02-05 |

---

## Halted: Missing Per-Repo Briefing

Planning for **FHIR-52745** stopped at **Step 3** of the `ticket-plan` skill
because a required per-repo briefing is missing.

### What the ticket targets

- **Title:** Choice Orientation extension should allow all choice types
- **Resolution description (verbatim):**
  > We will change the contextConstraint to this: `(type!='group' and
  > type!='display')`. Will add a note that this extension is only
  > meaningful if the item extended has `answerOption`, `answerValueSet`,
  > an `answerExpression`, or another extension that provides a list of
  > permitted answers.
- **FHIR artifact touched:** the `questionnaire-choiceOrientation`
  extension (context invariant + description prose).

### In-scope repositories

The `choice-orientation` extension does **not** live in `HL7/fhir`. A
case-insensitive search of `cache/github/repos/HL7_fhir/clone/source`
returned no matches for `choice-orientation` or `choiceOrientation`.
Searching the other cached clones located the StructureDefinition at:

- `HL7/fhir-extensions` — `input/definitions/multiple/StructureDefinition-questionnaire-choiceOrientation.xml`

`HL7/fhir-extensions` is therefore the primary in-scope repository for
this ticket. `HL7/fhir` is **not** expected to require edits for this
resolution (the extension was moved out of Core and is now maintained in
the extensions IG), but it would be reconfirmed from the briefing if one
existed.

### Briefing status

| Repo | Briefing present? | Clone HEAD | Notes |
|-|-|-|-|
| `HL7/fhir` | ✅ `cache/github/repos/HL7_fhir/repo-analysis/briefing.md` (fresh — clone HEAD `db79dcd` and playbook SHA `9c2e8e5` both match `meta.json`) | `db79dcdfe196d35bd0e74c58c655a4c1c8f534f7` | Scoped to FHIR-42980 but facts/authoring-root sections are generally applicable. |
| `HL7/fhir-extensions` | ❌ **Missing.** No `repo-analysis/briefing.md` or `meta.json` under `cache/github/repos/HL7_fhir-extensions/`. | `00df5f8371bcf6d4c897631ddba9f1fef3ed9b0b` | This is the repo that actually contains the extension being modified. |

Per the `ticket-plan` Step 3 rules, I must not fabricate repo facts
(authoring root, change recipes, gotchas, cross-repo touch points, etc.)
for `HL7/fhir-extensions`. The plan cannot be grounded in a Repo Context
that does not exist, so it is not produced here.

### Requested action

Please run the `repo-analysis` skill against `HL7/fhir-extensions` and
then re-run `ticket-plan FHIR-52745`:

```
repo-analysis HL7/fhir-extensions
```

Once `cache/github/repos/HL7_fhir-extensions/repo-analysis/briefing.md`
and `meta.json` exist (and `meta.json`'s `clone_head_sha` matches the
current clone HEAD `00df5f8`), I will resume and produce the full
Feature Proposal / Impact Analysis / Implementation Plan grounded in
that briefing.

### Data gathered so far (preserved for the resumed run)

- Ticket snapshot: `temp/plan/ticket.json`
- Cross-references: `temp/plan/xref.json` — no GitHub cross-references;
  one Zulip thread
  (`179255-questionnaire/Choice-orientation-extension for other types
  than Coding`) and a `fhir` target of
  `Questionnaire.item.answerOption`.
- Keywords: `temp/plan/keywords.json` — top terms: `orientation`,
  `choice`, `answerOption`, `Questionnaire.item.answerOption`,
  `contextConstraint`, `answerExpression`, `answerValueSet`.
- Extension file located in clone:
  `cache/github/repos/HL7_fhir-extensions/clone/input/definitions/multiple/StructureDefinition-questionnaire-choiceOrientation.xml`
  (contents intentionally not summarised here — that belongs in the
  Repo Context once the briefing is produced).
